---
id:                  # 파일명 번호와 동일 (예: 0002)
date:                # YYYY-MM-DD
branch:              # 대상 브랜치 (미정이면 비움)
ticket:              # 있으면 RP-xxxx, 없으면 비움 — 나중에 추가 가능
epic:                # 여러 태스크를 한 작업으로 묶는 라벨 (선택)
status: draft        # draft → approved → done
---
# <제목 — 무엇을 하는 작업인가>

> SRS(작업 요구사항 명세). 프롬프트 1건 = 이 문서 1개. 파일명: `specs/NNNN_<slug>.md`
> 본문의 자리표시자 `<...>` 를 모두 실제 내용으로 채워야 게이트를 통과합니다.
> 가장 중요한 산출물 — 이 문서만으로 작업을 재현(replay)할 수 있어야 합니다.

## 요청 (원문)
<사용자 프롬프트를 가능한 한 그대로. 의도가 드러나게.>

## 목표
<이 작업으로 무엇을 달성하는가>

## 수용 기준
- [ ] <검증 가능한 완료 조건>

## 범위 / 비고
- 포함:
- 제외:

## 승인
- [ ] 승인  (사람이 `! node .claude/hooks/srs-approve.mjs` 로 기록 — 에이전트 금지)
