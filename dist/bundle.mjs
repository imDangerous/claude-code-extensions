#!/usr/bin/env node
// ccx v2 — pack 단위 설치 (core→js→web/app), requires 자동 동반, 공유 config, apply.
//   ccx <pack> <init|check|doctor|update|remove>   ·   ccx apply   ·   ccx list
// 빌드 시 build.mjs 가 아래 placeholder 를 CATALOG 로 치환.
import { execFileSync, execSync } from 'node:child_process';
import { chmodSync, existsSync, mkdirSync, readFileSync, readdirSync, rmSync, writeFileSync } from 'node:fs';
import { createHash } from 'node:crypto';
import { basename, dirname, join, resolve } from 'node:path';
import { createInterface } from 'node:readline/promises';

const CATALOG = {"app":{"meta":{"name":"app","description":"RN 프레임워크 — Expo (baseline SDK 56)","requires":["js"],"concern":"frontend"},"modules":{"expo":{"manifest":{"name":"expo","deps":[],"husky":false,"questions":[],"targets":[{"src":"expo.md","dest":".claude/rules/expo.md","kind":"doc"},{"src":"app-inspector.md","dest":".claude/agents/app-inspector.md","kind":"doc"},{"src":"idea-researcher.md","dest":".claude/agents/idea-researcher.md","kind":"doc"},{"src":"product-planner.md","dest":".claude/agents/product-planner.md","kind":"doc"},{"src":"spec-planner.md","dest":".claude/agents/spec-planner.md","kind":"doc"},{"src":"design-architect.md","dest":".claude/agents/design-architect.md","kind":"doc"},{"src":"feature-builder.md","dest":".claude/agents/feature-builder.md","kind":"doc"},{"src":"ui-developer.md","dest":".claude/agents/ui-developer.md","kind":"doc"},{"src":"api-integrator.md","dest":".claude/agents/api-integrator.md","kind":"doc"},{"src":"skills/ideate/SKILL.md","dest":".claude/skills/ideate/SKILL.md","kind":"doc"},{"src":"skills/plan-app/SKILL.md","dest":".claude/skills/plan-app/SKILL.md","kind":"doc"}]},"files":{"api-integrator.md":"---\nname: api-integrator\ndescription: API 연동·서버/클라이언트 상태·analytics를 구현한다. Axios(토큰 자동 리프레시)+TanStack Query+Zustand, SecureStore 토큰 저장, 평점/광고 정책 래퍼. \"API 연동\", \"상태관리\", \"데이터 패칭\", \"analytics 붙여줘\" 요청 시 사용.\ntools: Read, Write, Edit, Glob, Grep, Bash\n---\n<!-- Managed by ccx -->\n\n# api-integrator — 데이터/서비스·상태 계층\n\n`architecture.md`의 **도메인/서비스 + hooks(ViewModel)** 계층을 구현한다. 프레젠테이션은 ui-developer 담당 — 이 계층은 `react-native` UI에 비의존(순수 서비스 + 상태 hook).\n\n## 역할\n- **API 클라이언트**(Axios) — 토큰 자동 리프레시, 토큰은 **SecureStore에서** 읽음(AsyncStorage 직접 조회 금지).\n- **서버 상태** = TanStack Query(query/mutation hook, query-key factory, refetch-on-focus). **클라이언트 상태** = Zustand.\n- **analytics·평점·광고** 래퍼 모듈 구현(아래 정책 준수).\n\n## 핵심 패턴\n- **서비스 함수(순수)**: API 호출/변환을 `features/{name}/api.ts` 또는 공용 `src/lib/api`에. UI import 0 → Node 테스트 가능.\n- **hook(ViewModel)**: `useXxx`가 Query/Mutation + Zustand를 묶어 state+핸들러 반환. 컴포넌트는 이 hook만 사용.\n- **SecureStore 래퍼**: `setSecureItem/getSecureItem/deleteSecureItem`(`expo-secure-store`, iOS `WHEN_UNLOCKED_THIS_DEVICE_ONLY`). 토큰 store는 Zustand `persist`의 `storage`에 **SecureStore-backed 어댑터** 주입(AsyncStorage 어댑터 금지).\n- 경로는 중립(전용 모듈) — FSD 레이어/barrel 강제 안 함(`architecture.md`).\n\n## Analytics 규칙 (해당 시)\n- 이벤트는 전용 래퍼 경유, 상수로 정의(`tap_{action}_{target}`·`complete_{flow}`, snake_case 동사_명사). **PII(이메일/전화/실명/정확위치) 파라미터 금지**, 토큰/PII 로그 노출 금지.\n- 화면 추적은 `useScreenTracking()` 같은 hook으로 일원화.\n\n## 평점 (Store Review) 정책 엔진\n- `expo-store-review`를 래퍼 밖에서 직접 호출 금지. 정책 엔진 `canRequestReview(ctx)`가 게이트 검사: `maxRequestsPerYear`·`cooldownAfterLaunchSec`·세션당 1회·최근 에러 없음·`uiIsIdle`. `maybeRequest`는 fire-and-forget(반환값 분기 금지). 상태(`sessionStartedAt`/`requestHistory`)는 1년 초과 prune.\n\n## 광고 (AdMob) 동의·정지 방어\n- **동의 시퀀스 고정**: `UMP → ATT(iOS) → mobileAds().initialize()`. `initializeAdsWithConsent()`만 `_layout`에서 await. `mobileAds().initialize()`/`AdsConsent.*`/`requestTrackingPermissionsAsync`를 래퍼 밖에서 직접 호출 금지.\n- **무효 트래픽 방어**(`ad.store`): 배너 클릭 가드(일 N회 초과 시 숨김), 전면 포맷 공유 쿨다운 + 일일 cap, 리워드는 `EARNED_REWARD` 콜백에서만 지급, 로드 실패 지수 백오프, preview/internal 빌드는 `testDeviceIdentifiers` 등록. `app.config.ts`에 plugin + `NSUserTrackingUsageDescription` 일치.\n- 콘솔 사전설정(GDPR/IDFA 메시지·광고 단위)은 운영 작업 — 자동화(Playwright MCP)는 선택, 실패 시 수동 절차로 fallback.\n\n## 핸드오프\n- feature-builder로부터: 기능 타입/구조. ui-developer에게: hook 사용 가이드. qa-reviewer에게: 타입 안전성·에러 핸들링 검증 요청.\n\n## Trigger\n\"API 연동\", \"상태관리/store\", \"데이터 패칭\", \"analytics/광고/평점 붙여줘\".\n","app-inspector.md":"---\nname: app-inspector\ndescription: 모바일 앱의 기능·UX·디자인 일관성·접근성을 종합 검수한다. PRD 대비 구현 완성도, 화면 흐름, 디자인 토큰 일치, 터치 접근성, 엣지 케이스를 점검. \"앱 검수\", \"기능 테스트\", \"UX 검토\", \"디자인 QA\" 요청 시 사용.\ntools: Read, Grep, Glob, Bash\n---\n<!-- Managed by ccx -->\n\n# app-inspector — 앱 종합 검수\n\n모바일 앱의 기능·UX·디자인을 종합 검수한다. 코드 품질/타입은 `qa-reviewer` 담당 — 이 에이전트는 **기능·UX·디자인·접근성**에 집중한다.\n\n## 컨텍스트 로드 (있으면 우선)\n- PRD/기획서, 디자인 시스템 문서가 있으면 먼저 Read하고 체크리스트로 변환한다. (예: `_workspace/02_product_plan.md`, `_workspace/03_design_system.md` 같은 산출물이 있으면 활용.)\n- 없으면 코드(`src/`, `app/`)에서 기능을 유추해 검수 기준을 자체 생성한다.\n- 명세에 **꺼진** 항목의 화면/모듈이 존재하면 오히려 FAIL로 보고. 제약 메모(\"온보딩 3장 이하\" 등)가 코드에 반영됐는지 grep으로 직접 검증.\n\n## 검수 체크리스트\n### NativeWind 무결성 (최우선 — CRITICAL)\n`babel.config.js`(jsxImportSource+preset)·`metro.config.js`(withNativeWind)·`tailwind.config.js`(preset+content)·`global.css`·루트 `_layout.tsx` import·`nativewind-env.d.ts` — 하나라도 누락이면 전체 UI가 깨지므로 CRITICAL.\n\n### 기능 완성도\n- PRD 유저 스토리 구현 여부 · happy path 동작 · API 실패/네트워크 오류 처리 · 로딩 상태 표시.\n\n### UX 흐름\n- 화면 간 네비게이션 연결 · 뒤로가기 자연스러움 · 빈 상태 처리 · 키보드가 입력 필드 가리지 않음.\n\n### 디자인 일관성\n- 컬러/타이포/스페이싱(4px 기반)이 디자인 시스템과 일치 · 컴포넌트 variant가 명세대로.\n\n### 접근성\n- 터치 타깃 ≥ 44×44px · 색상 대비 ≥ 4.5:1 · `accessibilityLabel` 설정.\n\n### 엣지 케이스\n- 긴 텍스트 레이아웃 · 이미지 로드 실패 placeholder · 빠른 연속 탭 방어.\n\n### 광고 배치 (해당 시)\n- 터치 요소와 ≥16dp 이격 · 화면당 배너 1개 · 미로드 시 collapse · 빈/에러/로딩 화면 비노출 · 전면 광고는 자연스러운 break point에서.\n\n## Hard Thresholds (하나라도 미달 시 FAIL)\n| 기준 | 임계 |\n|------|------|\n| PRD P0 유저 스토리 구현율 | 100% |\n| 모든 API 호출 에러 처리 | 누락 0 |\n| 스크린 SafeAreaView | 누락 0 |\n| 리스트 화면 빈 상태 | 누락 0 |\n| API 의존 화면 로딩 상태 | 누락 0 |\n| 프레젠테이션 컴포넌트의 직접 패칭/비즈니스 로직 (`architecture.md`) | 0 |\n| 도메인/유틸 모듈의 `react-native` import | 0 |\n\n## 디자인 평가 (4축)\nDesign Quality(30%, ≥7) · Originality(25%, ≥6) · Craft(25%, ≥7) · Functionality(20%, ≥8). Quality/Originality 가중으로 \"안전한 기본값\"이 아닌 심미적 도전을 유도.\n\n## 출력\n```markdown\n# 앱 검수 보고서\n## 종합 점수: {0~100}\n## 기능 완성도: {N}/{M} 유저 스토리\n| US-ID | 상태 | 비고 |\n## 발견 이슈\n### CRITICAL / WARNING / INFO\n- [파일:라인] 이슈 → 수정 방법\n```\n\n## 원칙\n- 정적 분석만 하지 않는다 — 실제 컴포넌트 파일을 Read하고 className/props/상태 처리를 확인한다.\n- API 타입 ↔ UI 타입, 스토어 상태 ↔ 화면 동기를 교차 비교한다.\n- 모든 판정에 구체적 근거(파일:라인)를 첨부한다.\n","design-architect.md":"---\nname: design-architect\ndescription: 앱 디자인 시스템과 UI/UX를 NativeWind/Tailwind 기반으로 설계한다. 컬러 팔레트, 타이포그래피, 컴포넌트 라이브러리, 화면 레이아웃, 다크모드 토큰을 정의한다. \"디자인 해줘\", \"디자인 시스템\", \"UI 설계\", \"색상 팔레트\", \"화면 디자인\" 요청 시 사용.\ntools: Read, Write, AskUserQuestion\n---\n<!-- Managed by ccx -->\n\n# design-architect — 디자인 시스템\n\n모바일 앱 디자인 시스템을 설계하고 감각적 UI를 NativeWind(Tailwind) 기반으로 정의한다.\n\n## 핵심 역할\n1. 디자인 시스템 — 컬러·타이포·스페이싱·라운딩 토큰\n2. 컴포넌트 라이브러리 — Button/Card/Input/Badge/Avatar variant\n3. 화면별 레이아웃 명세(와이어프레임 수준)\n4. 앱 톤/분위기 결정 · 다크모드(Light/Dark) 토큰\n\n## 컨텍스트 로드 (있으면 우선)\n- `_workspace/spec.md`의 `project`, `ux.dark_mode`/`languages`/`haptics`/`onboarding`/`empty_state_illustration`, `tech.animation_level`과 본인 영역 `*_notes`·`project.context`(톤앤매너).\n- `ux.dark_mode`로 토큰 범위 자동 조정(`light_only`/`dark_only`/`system_with_toggle`). `tech.animation_level=minimal`이면 Reanimated 의존 패턴 제거. 모순 모호 시 `AskUserQuestion`.\n\n## 작업 원칙\n- **NativeWind 중심** — 모든 스타일을 className으로. 커스텀 컬러/폰트/스페이싱은 `tailwind.config.js`에 정의, 테마 토큰은 전용 모듈(예: `src/lib/theme.ts`)에.\n- **일관성·모바일 퍼스트** — 토큰 기반 시각 언어, 터치 타깃 ≥44px, 접근성.\n- **감각적 디자인** — 트렌디하되 사용성을 해치지 않는 균형. spec의 `ux.*`/`tech.*`와 어긋나는 결정 금지.\n\n## NativeWind 인프라 검증 (CRITICAL — 디자인 작업 전 필수)\nclassName이 적용 안 되면 모든 디자인이 무의미하므로 **생략 불가**. `babel.config.js`(jsxImportSource+`nativewind/babel`)·`metro.config.js`(`withNativeWind`)·`tailwind.config.js`(preset+content `app/`·`src/`)·`global.css`·루트 `_layout.tsx` import·`nativewind-env.d.ts`를 확인. 누락 시 즉시 수정 후 `npx expo start -c`(캐시 클리어).\n\n## 출력 — `_workspace/03_design_system.md` (+ `tailwind.config.js`/테마 토큰 갱신)\n```markdown\n# 디자인 시스템: {앱 이름}\n## 브랜드 톤 — 분위기 / 레퍼런스 앱 2~3개\n## 컬러 팔레트\n| Token | Light | Dark | Usage |\n## 타이포그래피\n| Style | Size | Weight | Line Height | Usage |\n## 스페이싱 — 4px 기반: xs(4) sm(8) md(16) lg(24) xl(32) 2xl(48)\n## 컴포넌트 명세 (예: Button)\n- Variants: primary/secondary/outline/ghost/danger · Sizes: sm/md/lg · States: default/pressed/disabled/loading\n- NativeWind 예: `className=\"bg-primary-500 rounded-lg px-4 h-10 items-center justify-center\"`\n## 디자인 가드레일 (Do / Don't)\n- Do: \"primary는 CTA에만\", \"카드 라운딩은 rounded-2xl 고정\"\n- Don't: \"순수 #FFFFFF 배경 금지 → surface 토큰\", \"한 화면에 폰트 웨이트 3개 이상 혼용 금지\"\n## 화면별 레이아웃 — SafeAreaView > ScrollView > [...] 구조 + className 설명\n```\n\n## Grading Criteria (4축, 미달 시 재작업)\nDesign Quality(30%, ≥7) · Originality(25%, ≥6) · Craft(25%, ≥7) · Functionality(20%, ≥8). \"안전한 기본값\"보다 컨셉에 맞는 대담한 결정 권장 — Originality ≤5면 재작업.\n\n## 핸드오프\n- product-planner로부터: 화면 구조·기능 요구사항. idea-researcher로부터: 경쟁 앱 레퍼런스.\n- ui 구현 에이전트에게: 컴포넌트 명세·레이아웃. app-inspector에게: 디자인 가이드라인(QA 기준).\n\n## 에러 핸들링\n- 브랜드 방향이 모호하면 3가지 무드보드 옵션 제시. NativeWind로 어려운 디자인은 Reanimated 대안 제시.\n","expo.md":"<!-- Managed by ccx -->\n# Expo (React Native) — baseline SDK 56\n\n> Expo(managed) + React Native 앱의 AI 작업 표준. 버전 핀은 프로젝트 `package.json`이 SoT —\n> 룰은 가급적 SDK 무관한 **불변 규약**을 정의한다. 네이티브 의존성은 `npx expo install`로 호환 버전을 맞춘다(임의 `npm install` 금지).\n> baseline(공식 changelog 기준, 2026-05 — **프로젝트 설치 버전이 최종 SoT, 신규 API는 현행 문서로 확인**): SDK 56 ≈ RN 0.85 / React 19.2. New Architecture 기본(SDK 54~), SDK 55에서 Legacy 제거(New-Arch 호환 라이브러리만), Hermes v1 기본(SDK 56). UI/로직 분리는 `architecture.md` 따름.\n\n## 스택 (표준)\n- **Framework**: React Native + Expo (SDK 56, managed, New Architecture)\n- **Routing**: Expo Router (file-based, **typed routes** 활성 — 링크 타입 안전)\n- **State**: 전역 = Zustand · 서버 = TanStack Query\n- **Styling**: NativeWind (Tailwind for RN)\n- **Lists/애니메이션**: FlashList(v2) · Reanimated v4(+Gesture Handler, New Arch 필요)\n- **Media**: `expo-video`/`expo-audio` (`expo-av` 제거됨) · **Form**: React Hook Form + Zod\n- **Dates**: dayjs · **TypeScript**: strict\n\n## 프로젝트 구조 — 프레임워크 네이티브 + 가벼운 feature 그룹핑\n화면 구조의 SoT는 **Expo Router 파일 시스템**이다. 프레임워크와 싸우지 않는다.\n```\napp/                  # Expo Router — 파일 기반 라우팅 = 화면 구조 SoT (_layout.tsx, (tabs)/, [id].tsx)\nsrc/\n├── features/<도메인>/  # 기능 단위로 관련 코드를 colocation (ui·hooks·api·store 한곳에)\n├── components/         # 범용 재사용 UI\n└── lib/                # 유틸·api 클라이언트·secure-storage 래퍼 등\n```\n3가지 핵심 규칙 (React 공식 colocation·Redux 공식 feature folder·Bulletproof React(35k)·R.Wieruch이 뒷받침하는 주류):\n1. **`app/`는 얇게** — 라우트 파일(`app/(tabs)/index.tsx` 등)은 feature 컨테이너를 import해 **렌더만** 한다(`<FeedContainer/>`). 라우트 파일에 비즈니스 로직·스타일을 두지 않는다(화면 스위치 역할만).\n2. **기능 가방 colocation** — `feed` 기능에 필요한 ui·hooks·api·store·types를 `src/features/feed/` 한 폴더에 모은다. 전역 `components/`·`hooks/`·`api/`에 흩지 않는다.\n3. **기능 독립성(가급적)** — `features/feed`가 `features/profile` 내부를 **가급적** 직접 import하지 않는다. 두 곳 이상이 공유하면 기능 종속이 아니므로 **공용 위치로 올린다**(`src/components`·`src/lib`, 또는 `src/shared`). 단, 이를 깐깐히 강제하려 다층 레이어/barrel을 만들지 않는다 — 그건 FSD의 리팩토링 지옥(아래)이다.\n- import는 직접 경로(`@/` alias 가능)를 선호한다. `features/`는 **단순 폴더 그룹핑**일 뿐, 아래 FSD의 `features` *레이어*와 다르다 — 하위 폴더 구조를 강제하지 않는다.\n- 규칙 3은 **가벼운 2버킷 규칙**(feature-local ↔ 공용)이지, FSD의 다층 단방향 import 강제가 아니다(아래 DON'T 참고).\n\n## DON'T — Feature-Sliced Design(FSD) 금지 (CRITICAL)\n**이 프로젝트는 FSD를 채택하지 않는다.** `entities`/`features`/`widgets`/`shared` 같은 엄격한 레이어 분리와 단방향 import 규칙, 강제 barrel export(`index.ts` re-export 허브)를 도입하지 말 것. AI는 어떤 경우에도 FSD 구조를 제안·생성하지 않는다.\n- **보일러플레이트 폭발**: 버튼/API 하나에 `ui/ model/ api/ index.ts` 등 폴더 여러 개를 파게 돼 생산성이 급락한다.\n- **경계 논쟁 비용**: \"이건 feature냐 widget이냐 shared냐\"로 PR 리뷰 시간이 배가된다.\n- **리팩토링 지옥**: 동급 레이어 참조 금지 탓에 기능 A↔B가 데이터를 주고받아야 하면 상위로 억지로 끌어올리며 수십 개 import 경로를 뜯어고치게 된다.\n- **프레임워크 충돌**: 파일 기반 Expo Router와 FSD 레이어가 충돌해 진입점이 흐려지고, 강제 barrel export는 파일 추적·트리셰이킹을 망친다.\n- 이미 FSD로 깔린 코드를 만나면: 새 코드에 FSD를 확산시키지 말고, 위의 프레임워크 네이티브 구조로 점진 이전한다.\n\n## 코드 컨벤션\n- 프로덕션 코드에 `any` 금지. Interface=`I`, Type=`T`, Enum=`E` 프리픽스, 각각 별도 파일.\n- import alias `@/` 사용. 변경 후 lint·typecheck·format 실행.\n- 모든 스크린에 SafeArea 필수(`react-native-safe-area-context`). 리스트는 FlashList(v2: `estimatedItemSize` 불필요).\n\n## NativeWind 무결성 (CRITICAL)\n아래 중 하나라도 누락되면 `className`이 적용 안 돼 **전체 UI가 깨진다**:\n| 파일 | 필수 |\n|------|------|\n| `babel.config.js` | `['babel-preset-expo', { jsxImportSource: 'nativewind' }]` + `'nativewind/babel'` |\n| `metro.config.js` | `withNativeWind(config, { input: './global.css' })` |\n| `tailwind.config.js` | `presets: [require('nativewind/preset')]` + `content`에 `app/`,`src/` |\n| `global.css` | `@tailwind base; components; utilities;` |\n| 루트 `_layout.tsx` | `import '../global.css';` |\n| `nativewind-env.d.ts` | `/// <reference types=\"nativewind/types\" />` |\n\n## Secure Storage (MANDATORY)\n`AsyncStorage`는 평문 — 루팅/탈옥 기기에서 그대로 읽힌다. 민감 데이터는 `expo-secure-store`(iOS Keychain / Android Keystore).\n- **SecureStore 필수**: access/refresh 토큰, OAuth·세션 토큰, API 비밀키, 결제 토큰, 비밀번호/PIN, 라이선스 키, PII 결합 토큰.\n- **AsyncStorage/MMKV 허용(비민감만)**: 테마·언어·온보딩 플래그·비식별 캐시.\n- 토큰 접근은 **전용 SecureStore 래퍼 모듈**(예: `src/lib/secure-storage`)만 경유 — 토큰 직접 접근 코드 산재 금지. Zustand `persist`는 **비민감 슬라이스만** — 토큰 슬라이스를 AsyncStorage 어댑터로 저장 금지(SecureStore-backed 어댑터만).\n- iOS 기본 옵션 `WHEN_UNLOCKED_THIS_DEVICE_ONLY`(iCloud 백업 제외). 고위험 토큰은 `requireAuthentication: true`.\n- 토큰/PII를 `console.log`/Crashlytics/Analytics, `app.config.ts` `extra`, 클라이언트 `.env`에 노출 금지.\n\n## 날짜/시간 (CRITICAL)\n- 오늘 날짜는 **`dayjs().format('YYYY-MM-DD')`** — `new Date().toISOString().split('T')[0]` **금지**(UTC라 UTC+9에서 자정~09시 \"어제\" 반환).\n- N일 전: `dayjs().subtract(N, 'day')`. persist에는 `YYYY-MM-DD`/ISO 문자열만(Date 객체 금지).\n- `toISOString()`은 정렬/비교용 타임스탬프에만.\n\n## 수익화/스토어 안티패턴 (통합 시 — 즉시 FAIL 기준)\n> 상세 정책 엔진은 프로젝트 구현에 위임. 아래는 위반 시 FAIL인 경계.\n- **In-App Review**: 정책 엔진(`canRequestReview`, `maxRequestsPerYear`/`cooldown`/`uiIsIdle`) 미경유 평점 요청 금지. 자체 사전 프롬프트(\"평점 남겨주실래요?\")로 시스템 다이얼로그 유도 금지(정책 위반). `requestReview()`는 fire-and-forget(반환값 분기 금지). 에러/온보딩/결제 실패 직후 요청 금지.\n- **AdMob 동의 시퀀스**: `UMP → ATT(iOS) → mobileAds().initialize()` 순서 고정(위반 시 EU eCPM 폭락). `mobileAds().initialize()`·`AdsConsent.*`·`requestTrackingPermissionsAsync`를 **전용 ads 래퍼 모듈** 외부에서 직접 호출 금지. `app.config.ts`에 `NSUserTrackingUsageDescription` + plugin 문구 일치.\n- **무효 트래픽 방어**: 한 화면에 배너 1개, 로드 실패 시 컨테이너 collapse(빈 placeholder 금지), 숨김/가림/화면밖 광고 금지, 빈상태·에러·로딩·스플래시에 광고 금지. 전면 포맷은 공유 쿨다운+일일 cap 경유, 리워드 지급은 `EARNED_REWARD` 콜백 내부에서만.\n\n## EAS / 배포\n- 빌드·배포는 EAS 표준 절차. `.easignore`로 `_workspace/`·`.claude/`·테스트 산출물 제외. 앱 이름/식별자 일관성 유지.\n","feature-builder.md":"---\nname: feature-builder\ndescription: 새 기능 모듈을 feature-colocation 구조로 스캐폴딩한다. 기능 폴더에 ui·hooks·api·store·types를 모으고 UI/로직 분리를 적용한다. \"피처 만들어줘\", \"feature 추가\", \"새 기능 모듈\" 요청 시 사용.\ntools: Read, Write, Edit, Glob, Grep, Bash\n---\n<!-- Managed by ccx -->\n\n# feature-builder — 기능 모듈 스캐폴딩\n\n새 비즈니스 기능을 **feature-colocation** 구조로 생성한다. 구조는 `expo.md`(프레임워크 네이티브 + 가벼운 feature 그룹핑)와 `architecture.md`(UI/로직 분리)를 따른다.\n\n## 구조 (FSD 금지 — 레이어·barrel 강제 없음)\n```\nsrc/features/{name}/\n├── api.ts        # 데이터 접근(서비스) — 순수, UI 비의존\n├── hooks.ts      # ViewModel — 상태/사이드이펙트, api 호출, state+핸들러 반환\n├── types.ts      # 타입 (I/T/E 프리픽스)\n├── store.ts      # (선택) Zustand — 비민감 슬라이스만 persist\n└── components/   # (선택) 이 기능 전용 dumb 컴포넌트\n```\n- 라우트/화면은 `app/`(Expo Router)에 두고 feature 컨테이너를 import해 렌더만(`expo.md` 규칙 1).\n- **barrel(`index.ts` re-export 허브) 강제 금지**, entities/widgets/shared 레이어 금지. 직접 import 선호.\n- 두 기능 이상이 공유하면 공용으로 올린다(`src/components`·`src/lib`). 단 과분리 금지(`architecture.md`).\n\n## 규칙\n- `any` 금지. Interface=`I`/Type=`T`/Enum=`E`. import alias `@/`.\n- **UI/로직 분리 강제**: 비즈니스 규칙은 `api.ts`/순수 함수(=`react-native` import 0), 상태/이펙트는 `hooks.ts`, 컴포넌트는 props 받아 렌더만.\n- 토큰 등 민감 데이터는 SecureStore 래퍼 경유(`expo.md`).\n\n## spec 기반 분기 (있으면)\n`_workspace/spec.md`가 있으면 켜진 항목만 생성: `auth.methods=[]`→auth 미생성, `auth.methods≠[]`→auth + 계정 삭제 화면(Apple 5.1.1(v)), `ux.onboarding=none`→onboarding 미생성, `monetization`에 광고/IAP 포함 시 해당 기능 생성. `*_notes`가 객관식보다 우선, 모호 시 `AskUserQuestion`.\n\n## 핸드오프\n- api-integrator에게: 데이터/상태 계층 구현 요청. ui-developer에게: 화면/컴포넌트 요청. qa-reviewer에게: 생성 완료 검증 요청.\n\n## Trigger\n\"피처 만들어줘\", \"feature 추가\", \"새 기능 모듈\".\n","idea-researcher.md":"---\nname: idea-researcher\ndescription: 모바일 앱 아이디어를 리서치·제안한다. 인기 앱/시장 트렌드 조사, 경쟁 앱 벤치마킹, 페인포인트 발굴, 실현 가능한 아이디어 3~5개 제안. \"아이디어 줘\", \"앱 뭐 만들까\", \"트렌드 조사\", \"인기 앱 분석\" 요청 시 사용.\ntools: WebSearch, WebFetch, Read, Write\n---\n<!-- Managed by ccx -->\n\n# idea-researcher — 앱 아이디어 리서치\n\n모바일 앱 시장 트렌드를 분석하고 유망한 아이디어를 리서치해 제안한다.\n\n## 핵심 역할\n1. App Store / Google Play 인기·신규 앱 트렌드 조사\n2. 카테고리별 시장 분석 + 경쟁 앱 벤치마킹\n3. 사용자 페인포인트 발굴 → 기회 영역 식별\n4. 구체적·실현 가능한 아이디어 3~5개 제안\n\n## 컨텍스트 로드 (있으면 우선)\n- `_workspace/spec.md`가 있으면 `project.context`/`project.category`/`deployment.platforms`/`monetization.model`/`ux.languages`를 먼저 Read하고 방향성·제약으로 반영한다.\n- `*_notes` 자유 입력이 비어있지 않으면 같은 필드의 객관식 값보다 **우선**. 모순이면 `AskUserQuestion`(단 `execution.unattended:true`면 `on_ambiguity` 정책).\n- spec이 없으면 사용자의 관심 분야/카테고리 힌트로 진행하거나 Phase 0 설문을 제안한다.\n\n## 작업 원칙\n- **실현 가능성 우선** — 1인/소규모 팀이 Expo로 만들 수 있는 범위.\n- **차별점 명확화** — 기존 앱 대비 무엇이 다른지 반드시 명시.\n- **수익 모델 포함** — 각 아이디어에 수익 가능성(광고/구독/IAP). `monetization.model`이 정해졌으면 그에 맞춤.\n- **데이터 기반** — 추측이 아닌 실제 시장 데이터·트렌드 근거. WebSearch/WebFetch 적극 활용.\n\n## 출력 — `_workspace/01_idea_research.md`\n```markdown\n# 앱 아이디어 리서치 보고서\n## 시장 트렌드 요약\n- [트렌드 3~5개]\n## 경쟁 앱 분석\n| 앱명 | 카테고리 | 핵심 기능 | 약점/기회 |\n## 아이디어 제안\n### 아이디어 1: {이름}\n- 한줄 설명 / 타겟 사용자 / 핵심 기능 3가지 / 차별점 / 수익 모델 / 기술적 실현 가능성(높음·중간·낮음)\n```\n\n## 핸드오프\n- product-planner에게: 선정 아이디어 상세 컨텍스트.\n- design-architect에게: 경쟁 앱 디자인 레퍼런스(URL/스크린샷).\n- 리더로부터: 사용자가 선택한 아이디어 번호 수신.\n\n## 에러 핸들링\n- 웹 검색 실패 시 일반 지식으로 보완. 카테고리가 너무 넓으면 3개 세부 카테고리로 좁혀 질문.\n","product-planner.md":"---\nname: product-planner\ndescription: 모바일 앱 기획·PRD를 작성한다. 유저 스토리, 기능 목록·우선순위, 정보 구조, 화면 흐름(Expo Router), MVP 범위, 핵심 지표(KPI), 평점/광고 정책을 정의한다. \"기획해줘\", \"PRD 작성\", \"기능 정의\", \"화면 흐름 설계\" 요청 시 사용.\ntools: Read, Write, AskUserQuestion\n---\n<!-- Managed by ccx -->\n\n# product-planner — 앱 기획\n\n아이디어를 구현 가능한 제품 요구사항(PRD)으로 변환한다.\n\n## 핵심 역할\n1. PRD 작성 · 유저 스토리/시나리오 · 기능 목록·우선순위\n2. 정보 구조(IA) + 화면 흐름(User Flow) — **Expo Router 라우팅 그룹으로 매핑**\n3. MVP 범위 정의 (1차 출시 핵심 기능)\n4. **핵심 지표(KPI)** — 북극성 지표 1개 + 4축(획득/활성/유지/수익화), Analytics 이벤트로 매핑\n5. **Review Trigger 카탈로그** — 긍정 액션 2~5개 + 각 임계값. 안티패턴(첫 실행/온보딩/에러 직후)은 제외로 명문화\n\n## 컨텍스트 로드 (있으면 우선)\n- `_workspace/spec.md`가 있으면 `project`/`measurement`/`ux`/`monetization`/`auth`/`backend`/`permissions`/`policy`/`deployment`와 모든 `*_notes`·`project.context`를 Read.\n- `*_notes`가 비면 객관식보다 우선. [필수] 필드가 비면 중단하고 Phase 0 재실행 요청. 모순 모호 시 `AskUserQuestion`(`unattended:true`면 `on_ambiguity`).\n- **켜진(true) 항목만 PRD에 반영**: `ux.store_review` → Review Triggers 섹션 · `measurement.*analytics` → KPI 섹션 · `auth.methods≠[]` → 계정 삭제 흐름(Apple 5.1.1(v)) · `policy.ugc` → 신고/차단/모더레이션(Apple 1.2).\n\n## 작업 원칙\n- **MVP First** — 최소 핵심 기능으로 1차 정의, 확장은 이후.\n- **프레임워크 네이티브 구조** — 화면은 Expo Router `app/` 라우팅 그룹으로, 비즈니스 로직은 기능 단위로 가볍게 묶는다(colocation). **FSD 레이어(entities/widgets/shared)·단방향 import·강제 barrel로 분해하지 않는다.**\n- **구현 가능성 검증** — 현재 Expo(managed) 스택으로 가능한 범위.\n\n## 출력 — `_workspace/02_product_plan.md`\n```markdown\n# PRD: {앱 이름}\n## 제품 개요 — 한줄 설명 / 타겟 / 핵심 가치\n## 유저 스토리\n- US-001: 사용자로서, ~하기 위해 ~할 수 있다\n## 기능 목록\n| ID | 기능 | 설명 | 우선순위 |\n|----|------|------|---------|\n| F-001 | 로그인 | 이메일/소셜 | P0 |\n## 화면 구조 (Expo Router)\napp/\n├── (auth)/ login.tsx, signup.tsx\n├── (tabs)/ index.tsx(홈) ...\n└── (modal)/\n## 기능 ↔ 화면/모듈 매핑\n| 기능 | 화면(route) | 관련 코드 위치(예: src/features/{name}) |\n## MVP 범위 — 1차 출시 / 2차 확장 (기능 ID)\n## API 엔드포인트 (예상)\n| Method | Path | Description |\n```\n\n### KPI (해당 시) — Analytics 매핑\n- 북극성 지표 1개(정의+목표값) + 4축 기본 세트(획득 `first_open` / 활성 커스텀 `activation` / 유지 `session_start` / 수익화 `ad_impression`·`purchase`).\n- 커스텀 이벤트: `tap_{action}_{target}`, `complete_{flow}`. 규칙: snake_case 동사_명사, **PII 파라미터 금지**. 이벤트 상수는 전용 analytics 모듈에 정의(구현 에이전트가 처리).\n\n### Store Review Triggers (해당 시)\n- 후보 트리거 2~5개(트리거 액션·매핑 기능·임계값). 공통 게이트: 설치 후 ≥3일 · 실행 누적 ≥5회 · 마지막 요청 후 ≥90일 · 세션당 1회 · 최근 5분 내 에러/크래시 없음.\n- **안티패턴(요청 금지)**: 첫 실행/온보딩 중, 결제 실패·네트워크 오류·권한 거부 직후, 모달 표시 중.\n\n### Ads Strategy (monetization에 광고 포함 시)\n- placement 매트릭스(포맷·위치·빈도 정책·수익 임팩트). 공통 게이트: UMP consent 시퀀스 통과, 핵심 액션 직전/진행 중 광고 금지, 미로드 시 graceful fallback.\n- **안티패턴(배치 금지)**: 카메라/촬영·결제·온보딩·첫 실행·에러 직후, 핵심 액션 직전(Apple 4.5.4 강제 시청 금지).\n- **무효 트래픽 방어(계정 정지 방어)**: 배너 클릭 가드(일 N회 초과 시 숨김), 전면 포맷 공유 쿨다운, interstitial/rewarded 일일 cap, 세션당 전면 상한. 기본과 다르면 수치 명시 → 구현 에이전트가 config/게이트로 구현.\n\n## 핸드오프\n- idea-researcher로부터: 선정 아이디어 상세.\n- design-architect에게: 화면 구조·기능별 UI 요구사항.\n- 구현 에이전트에게: 기능별 타입 초안 / API 엔드포인트 목록 / 기능↔화면 매핑.\n- 리더로부터: 기획 승인/수정 요청.\n\n## 에러 핸들링\n- 아이디어가 모호하면 3가지 해석 제시 후 선택 요청. 기술적으로 불가능한 기능은 대안과 함께 명시.\n","skills/ideate/SKILL.md":"---\nname: ideate\ndescription: 앱 아이디어를 리서치하고 제안하는 스킬. 시장/트렌드·경쟁 앱 분석 후 실현 가능한 아이디어를 제시한다. \"아이디어 줘\", \"앱 뭐 만들까\", \"트렌드 조사\", \"어떤 앱 만들면 좋을까\" 요청 시 사용.\n---\n<!-- Managed by ccx -->\n\n# ideate — 앱 아이디어 리서치\n\n앱 아이디어를 체계적으로 리서치해 실현 가능한 후보를 제안한다. 실행은 `idea-researcher` 에이전트에 위임.\n\n## Trigger\n\"아이디어 줘\", \"앱 뭐 만들까\", \"어떤 앱이 좋을까\", \"트렌드/시장 조사\".\n\n## Steps\n1. **방향 설정** — 관심 카테고리(없으면 전체 트렌드), 타겟 시장(한국/글로벌), 개발 규모(1인/소규모) 확인.\n2. **트렌드 리서치** — WebSearch로 App Store/Play 인기·신규 트렌드, 페인포인트 많은 영역 탐색.\n3. **경쟁 앱 분석** — 상위 앱 5~10개 핵심 기능·강약점, 리뷰의 반복 불만 → 차별화 기회.\n4. **아이디어 도출** — 3~5개: 이름(가안)·한줄 설명·타겟·핵심 기능 3·차별점·수익 모델·Expo 실현 가능성.\n5. **선택·저장** — 후보 제시 → 사용자 선택/조합 → `_workspace/01_idea_research.md`에 저장.\n\n## 위임\n전 단계 `idea-researcher`. 다음 단계는 `plan-app`(기획/PRD).\n","skills/plan-app/SKILL.md":"---\nname: plan-app\ndescription: 앱 기획서(PRD)를 작성하는 스킬. 유저 스토리, 기능 목록, 화면 구조(Expo Router), 기능↔화면 매핑, API 엔드포인트, MVP 범위를 정의한다. \"기획해줘\", \"PRD 작성\", \"기능 정의\", \"화면 흐름 설계\", \"앱 설계\" 요청 시 사용.\n---\n<!-- Managed by ccx -->\n\n# plan-app — 앱 기획/PRD\n\n아이디어를 구현 가능한 PRD로 변환한다. 실행은 `product-planner` 에이전트에 위임. 구조는 `expo.md`(프레임워크 네이티브)·`architecture.md`(UI/로직 분리)를 따른다.\n\n## Trigger\n\"기획해줘\", \"PRD 작성\", \"기능 정의\", \"화면 구조/유저 스토리 설계\", \"앱 설계\".\n\n## Input\n`_workspace/01_idea_research.md`(있으면) 또는 사용자 설명.\n\n## Steps\n1. **요구사항 분석** — 핵심 가치·타겟 페르소나·핵심 vs 부가 기능 분류.\n2. **유저 스토리** — \"사용자로서 ~하기 위해 ~할 수 있다\", 우선순위 P0/P1/P2 + 인수 조건.\n3. **화면 구조** — Expo Router 그룹으로 매핑: `(auth)/`·`(tabs)/`·`(modal)/`·스택. 각 화면 주요 UI 요소.\n4. **기능 ↔ 화면/모듈 매핑** — 기능별 코드 위치를 **feature-colocation**으로(`src/features/{name}`: ui·hooks·api·store·types 한 폴더). **FSD 레이어(entities/widgets/shared)·강제 barrel로 분해하지 않는다**(`architecture.md`). 라우트 파일은 컨테이너 렌더만(얇게).\n5. **API 설계** — 엔드포인트 목록 + Request/Response 타입 초안 + 인증 방식.\n6. **MVP 범위** — 1차(P0)/2차(P1) → `_workspace/02_product_plan.md`에 저장.\n\n## 위임\n전 단계 `product-planner`. 다음: `spec-planner`(스펙/태스크) → `design-architect`(디자인) → 구현(feature-builder/api-integrator/ui-developer) → QA(qa-reviewer/app-inspector).\n","spec-planner.md":"---\nname: spec-planner\ndescription: PRD 기반으로 `docs/specs/`에 기능별 상세 스펙을 생성하고 phase/task 단위로 분해해 구현 진행을 추적한다. \"스펙 작성해줘\", \"specs 만들어줘\" 요청 시 사용.\ntools: Read, Write\n---\n<!-- Managed by ccx -->\n\n# spec-planner — 스펙/태스크 분해\n\nPRD를 기능별 스펙 문서로 분해하고 phase/task 체크리스트로 진행을 추적한다.\n\n## 입력 / 컨텍스트 로드 (있으면 우선)\n- `_workspace/02_product_plan.md`(PRD), 있으면 `_workspace/spec.md` 전 섹션 + `*_notes` + `project.context`.\n- spec에서 false/none/빈 배열 항목은 task에 포함하지 않는다(예: `ux.store_review=false`면 store-review task 생성 금지). `*_notes`가 비면 객관식보다 우선. [필수] 필드가 비면 Phase 0 재실행 요청.\n\n## 출력\n- `docs/specs/{NN}-{feature}/` (기능별, `NN`=우선순위 01·02…)\n- `docs/specs/README.md` (전체 진행 대시보드)\n\n## 프로세스\n1. PRD에서 구현 기능 목록 추출.\n2. 기능별 `docs/specs/{NN}-{feature}/` 디렉토리 생성.\n3. phase별 마크다운 작성: `phase1-mvp.md` / `phase2-enhancement.md` / `phase3-polish.md`.\n4. 각 phase에 체크박스 task 작성 — **프레임워크 네이티브 구조**로 경로 지정(기능 폴더 colocation + Expo Router 화면). FSD 레이어/barrel로 분해하지 않는다.\n5. `docs/specs/README.md` 대시보드 갱신(🔴 not started / 🟡 in-progress / 🟢 completed).\n\n### Task 예시 (Expo + 기능 폴더 colocation)\n```markdown\n---\nfeature: auth\nphase: 1\ntitle: MVP - 인증\nstatus: in-progress\ncreated: { YYYY-MM-DD }\nupdated: { YYYY-MM-DD }\n---\n# Phase 1: MVP - 인증\n## Tasks\n### 기능 (src/features/auth/ 에 colocation)\n- [ ] api — 로그인/회원가입 호출\n- [ ] hooks — useLogin / useRegister (mutation)\n- [ ] store — 토큰 관리(SecureStore-backed)\n- [ ] ui — LoginForm 컴포넌트\n- [ ] types — 인증 타입\n### 화면 (Expo Router)\n- [ ] app/(auth)/login.tsx\n- [ ] app/(auth)/register.tsx\n### QA\n- [ ] typecheck 통과 · lint 통과 · 기능 동작 확인\n```\n\n## Task 완료 처리 규칙\n구현 에이전트가 task 완료 시마다: ① 해당 spec 파일 `- [ ]`→`- [x]` ② phase 전체 완료 시 frontmatter `status: completed` ③ 대시보드 진행률 갱신. (구현 단계 전체에 적용.)\n\n## 에러 핸들링\n- PRD가 불충분하면 핵심 기능 3개를 확인 후 진행. phase 분리가 불명확하면 모두 Phase 1에 넣고 나중에 분리.\n","ui-developer.md":"---\nname: ui-developer\ndescription: NativeWind 기반 React Native UI 컴포넌트·스크린을 개발한다. Expo Router 화면/네비게이션, SafeArea, 애니메이션, 평점/광고 배선을 담당. \"UI 만들어줘\", \"컴포넌트 만들어줘\", \"스크린 추가\", \"레이아웃 수정\" 요청 시 사용.\ntools: Read, Write, Edit, Glob, Grep\n---\n<!-- Managed by ccx -->\n\n# ui-developer — UI/스크린 개발\n\nNativeWind 기반 RN 컴포넌트와 Expo Router 스크린을 만든다. **프레젠테이션 계층**(`architecture.md`): 컴포넌트는 props(데이터+콜백)를 받아 렌더만, 로직은 hooks/서비스에서 받아 연결.\n\n## 역할\n- 재사용 UI 컴포넌트(`src/components/`)와 기능 전용 컴포넌트(`src/features/{name}/components/`).\n- `app/`에 Expo Router 스크린·`_layout.tsx`(탭/스택). 스크린은 hook을 연결해 렌더(비즈니스 로직·직접 패칭 금지).\n- NativeWind className 스타일, SafeArea, FlashList, Reanimated v4 애니메이션. Bottom Sheet=`@gorhom/bottom-sheet`.\n\n## 규칙\n- 모든 스크린 SafeArea 필수(`react-native-safe-area-context`). className 사용(inline style 지양). 컴포넌트 PascalCase, props 타입 `I{Name}Props`. 리스트는 FlashList.\n- **UI/로직 분리**(`architecture.md`): 컴포넌트에 비즈니스 규칙·`fetch`/API 클라이언트 import 금지. `onClick`은 hook 핸들러 호출.\n- **NativeWind 무결성 선확인**(`expo.md`) — babel/metro/tailwind/global.css/layout import/타입 누락 시 즉시 수정.\n\n## 평점·광고 배선 (`expo.md` 안티패턴 준수)\n- 평점: `expo-store-review` 직접 호출 금지 → 전용 래퍼 `useStoreReview().maybeRequest(TRIGGER, { uiIsIdle: true })`를 **성공 콜백**에서만(토스트/애니메이션 끝나고 화면 idle일 때). 에러/catch 내부·모달 위 호출 금지. 반환값으로 분기 금지(fire-and-forget). 자체 사전 프롬프트·별점 유도 문구 금지.\n```typescript\n// REVIEW_TRIGGERS·maybeRequest·recordKeyAction 은 전용 store-review 래퍼(api-integrator 구현)에서 가져온다.\nconst { maybeRequest } = useStoreReview();\nconst recordKeyAction = useReviewStore((s) => s.recordKeyAction);\n\nconst onSaveSuccess = async () => {\n  recordKeyAction();\n  showToast('저장되었습니다');\n  await new Promise((r) => setTimeout(r, 400)); // UI idle 대기\n  await maybeRequest(REVIEW_TRIGGERS.AFTER_SAVE, { uiIsIdle: true });\n};\n```\n- 광고: 화면당 배너 1개, 인터랙티브 요소와 ≥16dp 이격, 미로드 시 컨테이너 collapse, 숨김/가림 렌더 금지, 빈/에러/로딩 화면 비노출. 전면은 `canShow*` 게이트 경유. 리워드는 opt-in 버튼 + 보상 명시. 광고 컴포넌트는 전용 래퍼(`AdBanner`)로만.\n\n## 핸드오프\n- api-integrator로부터: hook 사용 가이드 → 스크린에 연결. design-architect로부터: 레이아웃 + 가드레일(Do/Don't) 준수. qa-reviewer/app-inspector에게: 검증·UX 검수 요청.\n\n## Trigger\n\"UI/컴포넌트/스크린 추가\", \"화면 디자인\", \"레이아웃/스타일 수정\".\n"}}}},"backend":{"meta":{"name":"backend","description":"Spring 백엔드 — 구조·검증·관측·테스트 (Kotlin/Java variant)","requires":["core"],"concern":"backend"},"modules":{"observability-backend":{"manifest":{"name":"observability-backend","deps":[],"husky":false,"questions":[],"targets":[{"src":"observability-backend.md","dest":".claude/rules/observability-backend.md","kind":"doc"}]},"files":{"observability-backend.md":"<!-- Managed by ccx -->\n# 백엔드 관측성 (Micrometer · OpenTelemetry)\n\n> `observability.md` 원칙의 JVM 구현. 도구는 예시(동등 대체 가능).\n\n## 메트릭 / 추적\n- **Micrometer + Actuator** — `/actuator/health`·`/metrics`. RED(요청률·에러·지연) 메트릭(자동 + 커스텀 `@Timed`/`Counter`).\n- **분산 추적** — OpenTelemetry(또는 Micrometer Tracing)로 `traceId`/`spanId` 생성·전파(다운스트림 호출·메시징 헤더에).\n\n## 로깅\n- **구조적 로깅(JSON)** — logback/log4j2 JSON encoder. **MDC에 traceId/requestId** 넣어 로그-추적 상관.\n- 레벨 규율(error/warn/info/debug). **PII/시크릿 로깅 금지**(마스킹).\n\n## DON'T\n- Actuator 민감 엔드포인트(env·heapdump 등)를 인증 없이 노출.\n- 로그·메트릭 태그에 토큰/PII/고카디널리티 값.\n- 예외 삼키기(`catch` 후 무시) — 추적기/로그로 보고.\n"}},"spring":{"manifest":{"name":"spring","deps":[],"husky":false,"questions":[{"key":"lang","type":"string","default":"kotlin","prompt":"백엔드 언어 (kotlin/java)","flag":"lang"}],"targets":[{"src":"spring.md","dest":".claude/rules/spring.md","kind":"doc"},{"src":"spring-kotlin.md","dest":".claude/rules/spring-lang.md","kind":"doc","enabledIf":"lang","equals":"kotlin"},{"src":"spring-java.md","dest":".claude/rules/spring-lang.md","kind":"doc","enabledIf":"lang","equals":"java"}]},"files":{"spring-java.md":"<!-- Managed by ccx -->\n# Spring 언어 규약 — Java\n\n> `spring.md` 구조 규약의 Java 적용 (config `lang=java`).\n\n- **DTO**: `record`(불변). **엔티티엔 record 금지**(JPA 기본 생성자·가변성 필요).\n- **Optional**: 반환 타입에만(필드·파라미터 금지). null 반환 지양.\n- **불변 우선**: `final` 필드·방어적 복사.\n- **DI**: 생성자 주입(`final` 필드). Lombok `@RequiredArgsConstructor`는 팀 정책 따름.\n- **스트림/함수형**: 가독성 우선 — 과도한 체이닝·중첩 람다 지양(`entropy.md`).\n- 테스트 mock은 **Mockito**(`testing-backend`).\n","spring-kotlin.md":"<!-- Managed by ccx -->\n# Spring 언어 규약 — Kotlin\n\n> `spring.md` 구조 규약의 Kotlin 적용 (config `lang=kotlin`).\n\n- **null-safety**: 플랫폼 타입은 경계에서 명시. `!!` 금지 — `?:`/`requireNotNull`로 의도 표현.\n- **불변 우선**: `val`·읽기전용 컬렉션. DTO는 `data class`(변형은 `copy`).\n- **매핑**: 확장 함수(`fun Entity.toDto()`)로 엔티티↔DTO 변환.\n- **DI**: 주생성자 주입. `lateinit`는 테스트/프레임워크 주입 한정.\n- **JPA 엔티티**: `class`(데이터클래스 지양 — equals/hashCode·지연로딩 프록시 문제). `kotlin-jpa`(allopen/noarg) 플러그인.\n- **코루틴**(도입 시): 블로킹 호출 격리, 트랜잭션 경계·디스패처 주의.\n- 테스트 mock은 **MockK**(`testing-backend`).\n","spring.md":"<!-- Managed by ccx -->\n# Spring 백엔드 구조 (layered)\n\n> Spring Boot 백엔드 규약. 언어는 `.claude/extends/config.json`의 `lang`(kotlin/java). 빌드는 Gradle(Kotlin DSL) 기본 가정(Maven도 가능 — 도구 비종속). 일반 원칙은 `agent-workflow`·`validation`·`observability`·`entropy`(core 상속).\n\n## 계층 (단방향 의존)\n- `controller`(web 경계) → `service`(유스케이스·트랜잭션) → `repository`(영속). 역방향·계층 건너뛰기 금지.\n- **DTO 경계** — controller는 요청/응답 DTO로 입출력. **엔티티를 web에 직접 노출 금지**. 매핑은 명시적(매퍼/확장함수).\n- 도메인 로직은 service/도메인 모델에. controller는 얇게(검증·위임·응답).\n\n## DI / 의존\n- **생성자 주입**만(필드 `@Autowired` 금지). 협력자는 인터페이스로 추상화(테스트 대체 가능).\n- 컴포넌트 스캔 범위 명확히. 순환 의존 금지.\n\n## 트랜잭션\n- `@Transactional`은 **service 계층**에. 조회는 `readOnly = true`. controller/repository에 두지 않는다.\n- 트랜잭션 경계 안에서 외부 HTTP/메시징 호출 지양(롤백·지연 문제).\n\n## 예외 / 에러 응답\n- 도메인 예외 정의 → `@RestControllerAdvice` 전역 처리 → **일관된 에러 바디**(code·message·필드). 스택트레이스·내부 정보 노출 금지.\n\n## API / 설정\n- REST 규약·정확한 상태코드. 입력 검증은 `validation-backend`(`@Valid`). 페이징/정렬 표준화.\n- 프로파일별 설정(`application-{env}`). **시크릿은 환경변수/시크릿 매니저** — 소스·이미지·VCS에 금지.\n\n> 언어별 관용(kotlin/java)은 `spring-lang.md`(config `lang`에 따라 해당 언어만 배포).\n"}},"testing-backend":{"manifest":{"name":"testing-backend","deps":[],"husky":false,"questions":[],"targets":[{"src":"testing-backend.md","dest":".claude/rules/testing-backend.md","kind":"doc"}]},"files":{"testing-backend.md":"<!-- Managed by ccx -->\n# 백엔드 테스트 (JUnit5)\n\n> 구현이 아니라 **행동**을 검증. mock 도구는 `lang`에 따름 — kotlin → **MockK**, java → **Mockito**.\n\n## DO\n1. **JUnit5** 기본. 단위 테스트는 **스프링 컨텍스트 없이** 순수하게 — service 로직은 협력자를 mock으로 빠르게.\n2. **슬라이스 테스트** — `@WebMvcTest`(controller)·`@DataJpaTest`(repository). 전체 `@SpringBootTest`는 최소(느림).\n3. **통합/DB는 Testcontainers**(실제 DB 이미지). H2 대체는 DB 동작 불일치 주의.\n4. given-when-then, 의미 있는 단언(상태·상호작용). 외부 HTTP는 mock/WireMock.\n5. 도메인 불변식·검증·에러 경로(실패 케이스) 커버.\n\n## DON'T\n1. 모든 테스트를 `@SpringBootTest`로(느리고 깨지기 쉬움).\n2. 구현 디테일(private·내부 호출 횟수) 과결합 테스트.\n3. 운영 DB/외부 시스템 실호출. 커버리지 숫자 목표용 무의미 테스트.\n"}},"validation-backend":{"manifest":{"name":"validation-backend","deps":[],"husky":false,"questions":[],"targets":[{"src":"validation-backend.md","dest":".claude/rules/validation-backend.md","kind":"doc"}]},"files":{"validation-backend.md":"<!-- Managed by ccx -->\n# 입력 검증 — Bean Validation (Spring)\n\n> `validation.md` 원칙(경계에서 검증·fail-closed)의 JVM 구현.\n\n## DO\n1. **경계 DTO에 제약 애너테이션** — `@NotNull`·`@NotBlank`·`@Size`·`@Email`·`@Positive` 등. controller 파라미터에 `@Valid`/`@Validated`로 강제.\n2. **검증 실패 → 전역 처리** — `MethodArgumentNotValidException`/`ConstraintViolationException`을 `@RestControllerAdvice`에서 400 + 필드 에러로 매핑.\n3. **커스텀 제약** — 복합 규칙은 `ConstraintValidator` 구현. 단계 검증은 그룹/`@GroupSequence`.\n4. **이중 방어** — 도메인 불변식은 엔티티/값객체 **생성자**에서도 보장(영속 전 무효 상태 차단).\n5. 외부 연동 응답·메시지 페이로드도 신뢰 경계 — 역직렬화 후 검증.\n\n## DON'T\n1. service 깊은 곳에서 뒤늦게 검증(경계에서).\n2. `@Valid` 누락한 채 DTO 신뢰. 검증 우회 경로.\n3. 검증 안 된 입력을 엔티티에 바로 바인딩·영속.\n"}}}},"core":{"meta":{"name":"core","description":"표준 베이스 — git·에이전트·QA·작업흐름 (전 스택 공통)","requires":[],"concern":null},"modules":{"agent-workflow":{"manifest":{"name":"agent-workflow","deps":[],"husky":false,"questions":[],"targets":[{"src":"agent-workflow.md","dest":".claude/rules/agent-workflow.md","kind":"doc"}]},"files":{"agent-workflow.md":"<!-- Managed by ccx -->\n<!-- ccx:always -->\n# 에이전트 작업 흐름 (harness 엔지니어링)\n\n> AI 에이전트의 표준 작업 절차. 사람이 다시 지시하지 않아도 같은 품질로 재현되는 것이 목표.\n> 근거: Anthropic harness 엔지니어링(context engineering · gather→act→verify→iterate · generator≠evaluator) + Claude Code 베스트 프랙티스.\n\n## 루프 — gather → plan → act → verify → iterate\n모든 작업은 이 루프를 따른다. **매 행동 후 검증**하고, 막히면 무한히 돌지 말고 에스컬레이션한다.\n\n## 컨텍스트 엔지니어링\n1. **컨텍스트는 희소 예산** — 고신호 토큰만 올린다. 창이 찰수록 추론이 흐려진다(context rot). 큰 창보다 **가벼운 컨텍스트**가 신뢰도를 높인다.\n2. **광범위 탐색·다중 파일 조사는 서브에이전트에 위임** — 서브에이전트가 격리 탐색 후 **결론만** 반환(원본 파일·로그 덤프 금지). 메인 스레드는 단일 파일·문서·대화에 보존.\n3. **파일시스템을 기억으로** — 계획·진행상황·검증 결과를 파일로 남기고 필요할 때 당겨온다. 컴팩션에 기대 세션을 잇지 말고, 세션 경계는 **명시적 산출물**(진행 요약·검증된 테스트 결과·커밋된 git 상태)로 핸드오프.\n4. 무관한 작업 사이에는 컨텍스트를 비운다(`/clear`), 긴 작업은 컴팩션한다.\n\n## 검증 우선 (verification-first)\n5. **읽지 말고 실행해 검증** — 에이전트가 읽을 수 있는 신호(typecheck/lint/test/build/스크립트)를 매 행동 후 돌린다. 정적 분석만으로 통과 선언 금지.\n6. **객관 Hard Threshold로 완료 판정** — exit 0 · 테스트 100% · build SUCCESS 같은 pass/fail. \"괜찮아 보임\" 같은 주관 점수 금지.\n7. **주장 말고 증거** — 실제 출력(`42/42 PASSED`·exit code·diff)을 제시.\n8. **생성자와 평가자를 분리** — LLM은 자기 작업을 관대하게 평가한다. 검수는 새 컨텍스트/서브에이전트(예: qa-reviewer)로, 코드 정확성은 LLM 판단보다 코드 검증을 우선.\n\n## 계획·반복·게이트\n9. **계획과 실행을 분리** — explore → plan → implement → commit. 다중 파일 변경은 plan 모드/설계 문서 먼저. 3파일↑/다중 세션 작업은 `docs/plans/{날짜}-{slug}.md`에 저장.\n10. **모든 루프에 유한 상한 + 에스컬레이션** — 예: \"build 수정 최대 N회 후 중단/보고\". 무한 루프·비용 폭주 방지.\n11. **완료 기준을 먼저 선언** — \"테스트 통과 AND 린트 0 AND 리뷰됨\"처럼 \"done\"을 명시해 일찍 멈추거나 헛돌지 않게.\n12. **가정을 먼저 명시** — 해석이 갈리면 구현 전 확인. **작업 단위 = 커밋 단위**(\"그 김에 고친\" 코드는 별도 커밋).\n\n## Claude Code 프리미티브 — 올바른 도구 선택\n13. **skill / subagent / hook 구분** — *skill*(`.claude/skills/*/SKILL.md`)=재사용 절차(이름+설명로 점진 노출) · *subagent*(`.claude/agents/*.md`, frontmatter `description`/`tools`/`model`)=컨텍스트 격리 워커 · *hook*(PreToolUse/PostToolUse/Stop 등)=**매번 강제**돼야 하는 결정적 동작(자동 포맷·위험 명령 차단)은 CLAUDE.md 산문이 아니라 hook으로.\n14. **always-on 컨텍스트를 최소화**(핵심 원칙) — \"~200줄\"은 숫자 목표가 아니라 *always-on을 작게 유지하라*는 원칙(context rot 방지). CLAUDE.md엔 **전 작업에 적용되는 헌법급 원칙만** 상시 두고(ccx: `ccx:always` 마커 룰만 @import), 프레임워크/툴 상세는 **on-demand**(색인만 두고 해당 영역 작업 시 읽기)로 분리. 권한은 deny-first + 도구/파라미터 글롭으로 좁힌다.\n\n## DON'T\n- 문서 갱신 없이 코드만 쌓지 않는다 · 로컬 검증 없이 push·PR 하지 않는다(`--no-verify` 금지) · 계획 없이 대규모 리팩터(5파일↑) 시작하지 않는다 · \"작동한다\"를 실행 없이 선언하지 않는다.\n\n## 에스컬레이션\n변경이 계획의 2배↑ / 기존 테스트 3개↑ 수정 / 외부 API 불확실 / 보안·권한·개인정보 / 동일 증상 2회↑ 실패 → 자동 진행 말고 보고.\n"}},"entropy":{"manifest":{"name":"entropy","deps":[],"husky":false,"questions":[],"targets":[{"src":"entropy.md","dest":".claude/rules/entropy.md","kind":"doc"}]},"files":{"entropy.md":"<!-- Managed by ccx -->\n<!-- ccx:always -->\n# 복잡도/엔트로피 예산 (전 스택)\n\n> 코드 엔트로피(복잡도·중복·크기) 증가를 능동적으로 억제한다. 한도 초과는 분해 신호.\n\n## 예산 (기본 한도)\n| 항목 | 한도 |\n|------|------|\n| 파일 | ≤ 300줄 |\n| 함수/메서드 | ≤ 50줄 |\n| 순환복잡도(cyclomatic) | ≤ 10 |\n| 중첩 깊이 | ≤ 3 (초과 시 early return·추출) |\n| 중복 | 3회 이상 반복 시 추출 (단 AHA — 과추상화 금지, `architecture.md`) |\n\n## DO\n1. 한도 초과 시 **분해** — 함수 추출·모듈 분리. 한 함수 = 한 책임.\n2. early return으로 중첩 평탄화. 매직값은 명명 상수로.\n3. 추가 전에 삭제 — 죽은 코드·미사용 export 제거.\n\n## DON'T\n1. \"한 줄 더\" 누적으로 거대 파일/함수 방치.\n2. 한도 우회용 무의미 분할(가독성 해치는 과분할).\n\n## 측정·정합\n- 한도는 **기준선**(리뷰에서 근거 있으면 예외 가능, 기본은 거부). 하드 차단은 아님.\n- Biome `complexity` 그룹 `noExcessiveCognitiveComplexity`로 부분 자동화 가능 — 단 **cognitive ≠ cyclomatic**(의미 다름). ccx 기본 `biome.json`은 complexity off이며, 프로젝트가 켜서 강제할 수 있다.\n"}},"git":{"manifest":{"name":"git","description":"Conventional Commits + gitmoji + optional ticket prefix (커밋 컨벤션 — 전 스택). 훅/린트 구현은 js/git-hooks(Node).","deps":[],"husky":false,"questions":[{"key":"ticketPrefix","type":"string","default":"","prompt":"티켓 prefix (없으면 Enter)","flag":"ticket","validate":"^[A-Za-z0-9]*$","validateHint":"영문/숫자만 (예: RP) — 정규식 메타문자 불가"},{"key":"ticketRequired","type":"bool","default":false,"prompt":"티켓 필수?","flag":"ticket-required"}],"targets":[{"src":"rules/git.md","dest":".claude/rules/git.md","kind":"doc"}]},"files":{"rules/git.md":"<!-- Managed by ccx -->\n# Git 커밋/PR 규약 (ccx 관리)\n\n> **커밋 컨벤션은 전 스택 공통**(`core/git`). husky/commitlint/스크립트/CI 워크플로 **구현은 Node 전용 `js/git-hooks`** 모듈이 배포한다(JVM 등 비-Node 스택은 컨벤션만 적용, 훅은 스택 도구로).\n> 직접 수정 금지 — 값은 `.claude/extends/config.json`을 바꾸고 `ccx core git update`(컨벤션)·`ccx js git-hooks update`(훅)로 갱신, `ccx <pack> <module> doctor`로 점검.\n\n## 커밋 메시지\n\n입력은 **표준 conventional `type: 제목`**으로만. 훅이 자동으로 `[<TICKET>-n] gitmoji 제목`으로 변환한다\n(티켓 prefix 미설정 시 `gitmoji 제목`).\n\n```\n입력:  feat: 로그인 화면 추가        → 변환:  ✨ 로그인 화면 추가\n티켓 prefix 설정 시(브랜치 …/ABC-12-…):  → [ABC-12] ✨ 로그인 화면 추가\n```\n\n## type → gitmoji (표준 11개)\n\n| type | gitmoji | | type | gitmoji |\n|------|---------|-|------|---------|\n| feat | ✨ | | test | ✅ |\n| fix | 🐛 | | build | 📦 |\n| refactor | ♻️ | | ci | 👷 |\n| perf | ⚡ | | chore | 🔧 |\n| style | 💄 | | revert | ⏪ |\n| docs | 📝 | | | |\n\n표준 밖 type/이모지는 commit-msg 훅에서 차단된다.\n\n## PR 제목\n\n커밋과 동일 형식 `[<TICKET>-n] gitmoji 제목`. CI(`commit-standards.yml`)가 같은 commitlint 규칙으로 강제.\n\n## 설정 (`.claude/extends/config.json`)\n\n```jsonc\n{ \"ticketPrefix\": \"\", \"ticketRequired\": false, \"packageManager\": \"auto\",\n  \"prTitleCheck\": true, \"commitMsgCiCheck\": false }\n```\n"}},"git-branching":{"manifest":{"name":"git-branching","deps":[],"husky":false,"questions":[{"key":"gitBranching","type":"bool","default":false,"prompt":"Trunk-based 브랜치 전략 룰 추가?","flag":"git-branching"}],"targets":[{"src":"git-branching.md","dest":".claude/rules/git-branching.md","kind":"doc","enabledIf":"gitBranching"}]},"files":{"git-branching.md":"<!-- Managed by ccx -->\n# 브랜치 전략 — Trunk-Based Development\n\n> 단일 통합 브랜치(`main` = trunk)에 자주 통합한다. main은 항상 릴리스 가능 상태.\n\n## DO\n1. **짧은 수명 피처 브랜치** — main에서 분기, ≤ 1~2일 내 머지. 작은 PR.\n2. **빈번한 통합** — 매일 main에 머지. 큰 배치 머지 회피.\n3. **main 보호** — CI(typecheck/lint/test) 통과해야 머지. PR 경유(직접 push 지양).\n4. **미완성은 피처 플래그**로 격리(브랜치 장기화 대신).\n5. 릴리스는 main에서 태그(또는 짧은 릴리스 브랜치).\n\n## DON'T\n1. 장수 브랜치(`develop`·`release` 상시 유지 = GitFlow) — trunk-based에선 지양.\n2. 거대 PR·수일~수주 미머지 브랜치(머지 충돌·통합 지옥).\n3. main을 깨진 채 방치.\n\n> 조직/CI 정책이 다르면 프로젝트 `CLAUDE.md`에서 보강. (이 룰은 opt-in — `ccx core init --git-branching`)\n"}},"observability":{"manifest":{"name":"observability","deps":[],"husky":false,"questions":[],"targets":[{"src":"observability.md","dest":".claude/rules/observability.md","kind":"doc"}]},"files":{"observability.md":"<!-- Managed by ccx -->\n<!-- ccx:always -->\n# 관측성 (원칙 — 전 스택)\n\n> 운영 중 무슨 일이 일어나는지 알 수 있어야 한다. 로그·메트릭·추적 + 에러 추적.\n\n## DO\n1. **구조적 로깅** — JSON 등 구조화(메시지 + 컨텍스트 필드). 레벨 규율(error/warn/info/debug).\n2. **상관관계 ID** — request/trace id를 진입점에서 생성·전파(로그·다운스트림 호출에 포함).\n3. **에러 추적** — 예외는 추적기로 보고(스택 + 컨텍스트 + 요청/사용자 메타). 삼키지 않는다.\n4. **메트릭** — RED(Rate/Errors/Duration) 또는 USE. 헬스/레디니스 체크.\n5. **PII/시크릿 로깅 금지** — 토큰·비밀번호·개인정보 마스킹.\n\n## DON'T\n1. `console.log` 흩뿌리기(구조적 로거 사용) · 빈 `catch {}`(에러 무시).\n2. 고카디널리티/민감 값을 메트릭 라벨·로그에.\n3. 로그만 있고 추적/메트릭 없음.\n\n## 스택 도구\n- web/app: Sentry(에러) + web-vitals/RUM (`observability-web`).\n- Spring: Micrometer + OpenTelemetry + actuator (`observability-backend`).\n"}},"orchestrate":{"manifest":{"name":"orchestrate","deps":[],"husky":false,"questions":[],"targets":[{"src":"SKILL.md","dest":".claude/skills/orchestrate/SKILL.md","kind":"doc"}]},"files":{"SKILL.md":"---\nname: orchestrate\ndescription: 장기 작업을 단계(phase)로 분해하고 Go/No-Go 게이트와 자기개선 루프로 감독한다. plan→build→QA→iterate.\n---\n<!-- Managed by ccx -->\n\n# orchestrate\n\n여러 에이전트/스킬을 **감독(supervisor)** 으로 묶어 장기 작업을 수행한다. 프레임워크 무관 — 구현 단계는 설치된 스택 pack(web/app/…)에 위임.\n\n## 파이프라인 (단계별 핸드오프)\n```\nplan → design → build(스택 pack) → QA(qa-reviewer) → [FAIL] fix(≤N) → [PASS] 다음 → ship\n```\n각 단계 산출물을 `_workspace/{phase}.md`에 저장 → 컨텍스트 리셋돼도 파일로 이어간다(Context Reset > Compaction).\n\n## 견고성 장치 (Anthropic harness 설계 원칙)\n1. **객관 게이트** — 단계 전환은 qa-reviewer의 Hard Threshold PASS일 때만.\n2. **유한 반복** — QA FAIL → 수정 → 재QA, 최대 N회 후 escalate.\n3. **능동 테스트** — 읽지 말고 실행(typecheck/lint/test/build).\n4. **지속 기억** — `_workspace/` 파일 핸드오프 + `pipeline-status.md`.\n5. **회의적 평가자** — qa-reviewer를 관대하지 않게(few-shot·skepticism).\n6. **메타 재평가** — 모델/SDK 릴리스마다 스캐폴딩 재평가·가지치기.\n\n## concern 연동\n설치된 framework pack의 `concern`(frontend/backend)에 따라 동원 자산이 달라진다 — **frontend**: 기획→design→build→inspect 에이전트 파이프라인(idea/product/spec-planner·design-architect·feature-builder·api-integrator·ui-developer·inspector). **backend**: 현재 룰 기반(spring·validation/observability/testing-backend) — 검수 에이전트는 추후.\n\n## 사용\n사용자 요청 → 단계로 분해 → 각 단계 에이전트/스킬 호출 → 게이트 통과 시 진행 → 막히면 보고.\n"}},"qa-reviewer":{"manifest":{"name":"qa-reviewer","deps":[],"husky":false,"questions":[],"targets":[{"src":"qa-reviewer.md","dest":".claude/agents/qa-reviewer.md","kind":"doc"}]},"files":{"qa-reviewer.md":"---\nname: qa-reviewer\ndescription: 변경분을 객관적 Hard Threshold로 검수한다. 하나라도 미달이면 FAIL. 주관 점수 금지.\ntools: Bash, Read, Grep, Glob\n---\n<!-- Managed by ccx -->\n\n# qa-reviewer\n\n코드/문서 변경을 **객관 pass/fail 기준(Hard Threshold)** 으로만 검수한다. \"대체로 좋아 보임\" 같은 관대한 판정 금지 — **명시적 회의주의**로 본다.\n\n## 원칙\n- **Hard Threshold**: 하나라도 임계 미달이면 전체 FAIL. 소프트 점수 없음.\n- **능동 테스트**: 읽지 말고 **실행**(typecheck/lint/test/build). 정적 분석만으로 통과 선언 금지.\n- **few-shot 회의주의**: AI 산출물을 기본 신뢰하지 않는다. 근거(명령 출력·라인)로만 판정.\n\n## 기본 Hard Threshold (프로젝트가 추가/조정)\n| 기준 | 임계 | 측정 |\n|------|------|------|\n| 타입 오류 | 0 | `typecheck` |\n| 린트 에러 | 0 | `lint` |\n| 테스트 실패 | 0 | `test` |\n| 빈 상태(empty) 미처리 | 0 | 코드 분석 |\n| 에러 무시(`catch {}`) | 0 | 코드 분석 |\n| PII/시크릿 로깅 | 0 | grep |\n\n## 출력\n- 각 기준: PASS / FAIL + 근거(명령 출력 또는 파일:라인).\n- 하나라도 FAIL → **전체 FAIL**, 수정 후 재검수(기본 최대 3회, 프로젝트가 조정 가능; 초과 시 escalate).\n"}},"validation":{"manifest":{"name":"validation","deps":[],"husky":false,"questions":[],"targets":[{"src":"validation.md","dest":".claude/rules/validation.md","kind":"doc"}]},"files":{"validation.md":"<!-- Managed by ccx -->\n<!-- ccx:always -->\n# 입력 검증 (원칙 — 전 스택)\n\n> 모든 신뢰 경계에서 외부 입력을 검증한다. 검증되지 않은 데이터는 신뢰하지 않는다(fail-closed).\n\n## DO\n1. **경계에서 검증** — 요청/폼/쿼리/헤더/env/파일/외부 API 응답 등 모든 외부 입력을 진입 경계에서 스키마로 검증·정규화.\n2. **parse, don't validate** — 검증과 동시에 타입이 좁혀진 값을 만든다(검증 통과 = 타입 보장).\n3. **fail-closed** — 검증 실패는 명시적 거부(기본 거부). 누락/모호를 통과시키지 않는다.\n4. **신뢰 경계 명시** — 무엇이 신뢰/비신뢰 입력인지 문서화. 서버 권한·인가는 경계마다 **독립** 확인.\n5. 출력도 경계에서 인코딩/이스케이프(XSS·인젝션 방지). 스키마는 한 곳을 SoT로.\n\n## DON'T\n1. 클라이언트 검증만 신뢰 — 서버에서 재검증 필수.\n2. 부분 검증·검증 우회 경로·조용한 강제변환(silent coercion).\n3. 검증 로직 산재(중복 스키마).\n\n## 스택 구현\n- JS/TS: zod (`validation-zod` — RN/web 공유).\n- Spring: Bean Validation(`@Valid`/JSR-380) (`validation-backend`).\n"}}}},"js":{"meta":{"name":"js","description":"JS/TS 공통 — 린터·포매터·타입 (web/app 의 언어 베이스)","requires":["core"],"concern":null},"modules":{"architecture":{"manifest":{"name":"architecture","deps":[],"husky":false,"questions":[],"targets":[{"src":"architecture.md","dest":".claude/rules/architecture.md","kind":"doc"}]},"files":{"architecture.md":"<!-- Managed by ccx -->\n<!-- ccx:always -->\n# 프론트엔드 아키텍처 — UI/로직 분리 (Humble View + hooks as ViewModel)\n\n> 컴포넌트 기반 UI(React·React Native)에서 **비즈니스 로직이 UI를 직접 다루지 않게** 한다. UI는 자주 바뀌므로 갈아끼울 수 있어야 하고(swappable), 로직은 렌더러 없이 테스트 가능해야 한다.\n> 근거: Fowler **Humble Object/Presentation Model**, React 공식 \"custom hooks = stateful logic 공유\", **headless 컴포넌트**(Radix·TanStack·React Aria). MVC/MVP/MVVM이 공유하는 원칙의 현대 React 구현.\n\n## 3계층 — 각 계층은 한 가지 일만, 안쪽으로만 의존\n1. **도메인/서비스 (순수·프레임워크 무관)** — 비즈니스 규칙·검증·계산·포매팅 + 데이터 접근(API·storage 클라이언트).\n   - **`react`/`react-native`/`next`를 import하지 않는다.** 이 제약이 테스트성과 web↔RN 이식성의 핵심(=Humble Object). 같은 코드가 web·RN·Node 테스트에서 그대로 돈다.\n2. **hooks (\"ViewModel\")** — 상태·사이드이펙트·오케스트레이션. `useState`/`useEffect`/데이터패칭을 소유하고 도메인/서비스를 호출, 화면에 줄 **state + 핸들러**를 반환. (MVVM의 ViewModel / Presentation Model의 현대 구현 — 컨테이너 컴포넌트를 대체.)\n3. **프레젠테이션 (dumb 컴포넌트)** — props(데이터+콜백)를 받아 **렌더만** 하고 이벤트를 위로 emit. 패칭·비즈니스 규칙·API 클라이언트 import 없음(=Humble/Passive View → 갈아끼우기 쉬움).\n\n> **headless**는 이 분리의 productized 형태: 동작/접근성/상태는 hook(`useDropdown`, TanStack Table, React Aria)이, 렌더는 소비자가. \"로직은 라이브러리, UI는 너\".\n> **React Native 동일**: 도메인·hook 계층이 `react-native`를 import하지 않으므로 web과 그대로 공유, 프레젠테이션(`View`/`Text` vs DOM)만 다르다.\n\n## DO (각 항목 검증 가능)\n1. 비즈니스 규칙·검증·계산은 `domain/`(또는 `lib/`)의 **순수 함수**로. → 그 파일들은 `react`/`react-native`/`next` import 0 (ESLint `no-restricted-imports`로 강제).\n2. 데이터 패칭·사이드이펙트는 **hook 또는 서비스**에. 프레젠테이션 컴포넌트엔 `fetch`/`axios`/query-client import·I/O `useEffect` 없음.\n3. 컴포넌트는 props로 데이터+콜백을 받고 이벤트를 emit. dumb 컴포넌트는 도메인 모듈·API 클라이언트를 직접 import하지 않는다.\n4. 도메인 로직은 렌더러 없이 테스트(Node 단위 테스트, jsdom/RTL 불필요 — 빠르다).\n5. 복잡한 인터랙티브 위젯은 **headless 라이브러리**(Radix·TanStack·React Aria·Headless UI)로 접근성+스타일 교체성 확보.\n6. hook은 기능 폴더에 colocation. 한 컴포넌트만 쓰는 일회성 hook은 그 옆에 둬도 된다.\n\n## DON'T (즉시 교정 대상)\n1. **JSX/이벤트 핸들러 안에 비즈니스 로직 금지** — `onClick`은 가격/권한을 인라인 계산하지 말고 hook/서비스 핸들러를 호출.\n2. **프레젠테이션 컴포넌트에서 직접 패칭 금지** — 루프/누수 위험 + 재사용·테스트 불가.\n3. **컴포넌트가 API 클라이언트/axios/query-client를 직접 import 금지** — 서비스 계층 경유(HTTP 라이브러리 교체가 컴포넌트를 건드리지 않게).\n4. **도메인/유틸이 `react`/`react-native`/`next` import 금지** — 이식성·테스트성 파괴.\n5. **컨테이너/프레젠테이션 래퍼 컴포넌트로 분리하지 말 것** — 폐기된 방식(Dan Abramov 본인 철회). custom hook이 그 역할을 더 가볍게 한다.\n6. `useMount` 류 lifecycle 래핑 hook 금지(react.dev 권고) — 구체적 use case 단위 hook으로.\n\n## 과분리 경고 — 분리하되 과하게 추상화하지 않는다 (가장 중요한 뉘앙스)\n- **AHA** (Kent C. Dodds): \"잘못된 추상화보다 중복이 낫다\". 공통점이 \"소리칠 때\"까지 기다렸다 추상화.\n- **Rule of Three**: 실제 중복 ~3회 전엔 추출하지 않는다.\n- React 공식: \"작은 중복마다 custom hook을 뽑을 필요 없다 — 약간의 중복은 괜찮다.\" 일회성 hook 추출은 가독성을 되레 떨어뜨린다.\n- **풀 Clean/Hexagonal(ports/adapters·DI)은 프론트에서 niche/과설계** — 대부분은 *서비스 계층 + custom hooks + dumb 컴포넌트*면 충분. 거대·장수명·복잡 도메인(핀테크/엔터프라이즈)에서만 더 간다.\n- 휴리스틱: **세 책임(렌더 vs 오케스트레이션 vs 비즈니스 규칙)은 항상 개념적으로 구분**하되, 재사용·테스트성·복잡도가 요구할 때만 별도 파일/계층으로 승격.\n\n## 출처\n- martinfowler.com/bliki/HumbleObject.html · /eaaDev/PresentationModel.html · /articles/headless-component.html\n- react.dev/learn/reusing-logic-with-custom-hooks · kentcdodds.com/blog/aha-programming\n- radix-ui.com · tanstack.com · react-aria.adobe.com\n"}},"biome":{"manifest":{"name":"biome","deps":["@biomejs/biome"],"husky":false,"questions":[],"targets":[{"src":"biome.json","dest":"biome.json","kind":"static","createOnly":true},{"src":"biome.md","dest":".claude/rules/biome.md","kind":"doc"}]},"files":{"biome.json":"{\n  \"$schema\": \"https://biomejs.dev/schemas/2.5.0/schema.json\",\n  \"vcs\": { \"enabled\": true, \"clientKind\": \"git\", \"useIgnoreFile\": true },\n  \"files\": {\n    \"ignoreUnknown\": true,\n    \"includes\": [\n      \"**\",\n      \"!**/node_modules\",\n      \"!**/dist\",\n      \"!**/.next\",\n      \"!**/build\",\n      \"!**/coverage\",\n      \"!**/playwright-report\",\n      \"!**/test-results\",\n      \"!**/.claude\",\n      \"!commitlint.config.cjs\",\n      // Tailwind 지시어/생성 CSS를 Biome 포매터가 재포맷해 드리프트 유발 → CSS 제외(특히 v4 @theme)\n      \"!**/*.css\",\n      \"!**/pnpm-lock.yaml\"\n    ]\n  },\n  \"assist\": { \"actions\": { \"source\": { \"organizeImports\": \"on\" } } },\n  \"formatter\": {\n    \"enabled\": true,\n    \"indentStyle\": \"space\",\n    \"indentWidth\": 2,\n    \"lineWidth\": 100,\n    \"formatWithErrors\": false\n  },\n  \"javascript\": {\n    \"formatter\": {\n      \"quoteStyle\": \"single\",\n      \"jsxQuoteStyle\": \"double\",\n      \"semicolons\": \"always\",\n      \"trailingCommas\": \"all\",\n      \"arrowParentheses\": \"always\"\n    }\n  },\n  \"json\": {\n    \"formatter\": { \"indentWidth\": 2 }\n  },\n  \"linter\": {\n    \"enabled\": true,\n    \"rules\": {\n      \"preset\": \"recommended\",\n      \"a11y\": { \"preset\": \"recommended\" },\n      \"correctness\": {\n        \"preset\": \"recommended\",\n        \"noUnusedImports\": \"error\",\n        \"noUnusedVariables\": \"warn\"\n      },\n      \"style\": {\n        \"noDefaultExport\": \"off\",\n        \"useImportType\": \"error\",\n        \"useNodejsImportProtocol\": \"error\"\n      },\n      \"suspicious\": {\n        \"preset\": \"recommended\",\n        \"noConsole\": { \"level\": \"error\", \"options\": { \"allow\": [] } },\n        \"noExplicitAny\": \"error\"\n      },\n      \"complexity\": { \"preset\": \"none\" }\n    }\n  },\n  \"overrides\": [\n    {\n      \"includes\": [\"**/tests/**/*\", \"**/e2e/**/*\", \"**/*.test.ts\", \"**/*.test.tsx\", \"**/*.spec.ts\"],\n      \"linter\": {\n        \"rules\": {\n          \"suspicious\": { \"noConsole\": \"off\", \"noExplicitAny\": \"off\" }\n        }\n      }\n    },\n    {\n      \"includes\": [\n        \"**/scripts/**/*\",\n        \"**/next.config.*\",\n        \"**/tailwind.config.*\",\n        \"**/postcss.config.*\",\n        \"**/vitest.config.*\"\n      ],\n      \"linter\": {\n        \"rules\": {\n          \"suspicious\": { \"noConsole\": \"off\" }\n        }\n      }\n    },\n    {\n      // 정적 리스트형 컴포넌트가 많아 index를 key로 쓰는 패턴 의도적 허용\n      \"includes\": [\"src/components/**/*\"],\n      \"linter\": {\n        \"rules\": {\n          \"suspicious\": { \"noArrayIndexKey\": \"off\" }\n        }\n      }\n    }\n  ]\n}\n","biome.md":"<!-- Managed by ccx -->\n# Biome (린트·포맷·import 정렬)\n\n> 린트·포맷의 단일 도구는 Biome. ESLint·Prettier 사용 금지.\n\n## DO\n1. `biome check --write`(lint+fix)·`biome format --write`를 단일 도구로.\n2. a11y 규칙(`a11y.recommended`) 활성.\n3. import 정렬도 Biome(`organizeImports`)로 일원화.\n4. CI/pre-commit은 lint-staged → `biome check --write` (변경 파일만).\n\n## DON'T\n1. ESLint·Prettier·`eslint-plugin-*` 추가 금지.\n2. `biome`+`eslint` 혼용 금지.\n3. Biome가 잡을 규칙을 커스텀 스크립트로 대체하지 않는다.\n\n## 메모\n- 스캔 제외: `node_modules`·빌드 산출·`.claude`·생성물.\n- 버전 업: `biome migrate --write`로 설정 자동 승격.\n- ccx 관리 루트 설정 파일은 Biome 스캔에서 제외(포매터 재포맷으로 인한 드리프트 방지).\n"}},"git-hooks":{"manifest":{"name":"git-hooks","deps":["@commitlint/cli","@commitlint/config-conventional","husky"],"husky":true,"questions":[{"key":"packageManager","type":"string","default":"auto","prompt":"패키지 매니저 (auto/pnpm/npm/yarn/bun)","flag":"pm"},{"key":"prTitleCheck","type":"bool","default":true,"prompt":"PR 제목 CI 켤까?","flag":"no-pr-title","flagInverts":true},{"key":"commitMsgCiCheck","type":"bool","default":false,"prompt":"CI에서 커밋 메시지도 검사?","flag":"commit-ci"}],"targets":[{"src":"scripts/git/gitmoji-map.cjs","dest":".claude/extends/core/git/gitmoji-map.cjs","kind":"static"},{"src":"scripts/git/gitmoji-commit.cjs","dest":".claude/extends/core/git/gitmoji-commit.cjs","kind":"static","exec":true},{"src":"commitlint.config.cjs","dest":"commitlint.config.cjs","kind":"static"},{"src":"husky/prepare-commit-msg","dest":".husky/prepare-commit-msg","kind":"hook","blockId":"core/git","soleOwner":true},{"src":"husky/commit-msg","dest":".husky/commit-msg","kind":"hook","blockId":"core/git","soleOwner":true,"placeholders":{"__COMMITLINT__":"@bin:commitlint"}},{"src":"workflows/commit-standards.yml","dest":".github/workflows/commit-standards.yml","kind":"static","enabledIf":"prTitleCheck","placeholders":{"__INSTALL_CMD__":"@ciInstall"},"blocks":[{"name":"commit-messages","enabledIf":"commitMsgCiCheck"}]}]},"files":{"commitlint.config.cjs":"// Managed by ccx. Do not edit — change .claude/extends/config.json instead.\n//\n// 표준 Conventional Commits 룰(config-conventional)을 상속하고,\n// 헤더 형식만 `[PREFIX-n] gitmoji 제목`으로 재정의한다.\nconst { EMOJIS, TICKET_PREFIX, TICKET_REQUIRED } = require('./.claude/extends/core/git/gitmoji-map.cjs');\n\nconst alt = EMOJIS.map((e) => e.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&')).join('|');\n\n// 티켓 prefix 설정 여부/필수 여부에 따라 헤더패턴 구성\nlet ticketPart = '';\nlet hasTicketGroup = false;\nif (TICKET_PREFIX) {\n  const group = `\\\\[(${TICKET_PREFIX}-\\\\d+)\\\\] `;\n  ticketPart = TICKET_REQUIRED ? group : `(?:${group})?`;\n  hasTicketGroup = true;\n}\n\nconst headerPattern = new RegExp(`^${ticketPart}(${alt}) (.+)$`, 'u');\nconst headerCorrespondence = hasTicketGroup\n  ? ['ticket', 'type', 'subject']\n  : ['type', 'subject'];\n\nmodule.exports = {\n  extends: ['@commitlint/config-conventional'],\n  parserPreset: { parserOpts: { headerPattern, headerCorrespondence } },\n  rules: {\n    'type-enum': [2, 'always', EMOJIS],\n    'type-case': [0], // 이모지는 대소문자 개념 없음\n  },\n};\n","husky/commit-msg":"#!/usr/bin/env sh\n# >>> ccx:__CCX_ID__ (managed: do not edit between markers) >>>\n__COMMITLINT__ --edit \"$1\"\n# <<< ccx <<<\n","husky/prepare-commit-msg":"#!/usr/bin/env sh\n# >>> ccx:__CCX_ID__ (managed: do not edit between markers) >>>\nnode .claude/extends/core/git/gitmoji-commit.cjs \"$1\" \"$2\"\n# <<< ccx <<<\n","scripts/git/gitmoji-commit.cjs":"#!/usr/bin/env node\n// Managed by ccx. Do not edit — change .claude/extends/config.json instead.\n//\n// prepare-commit-msg: `type: 제목`(conventional) → `[PREFIX-n] gitmoji 제목`.\n//   - ticketPrefix 설정 시: 헤더에 있으면 보존, 없으면 브랜치명에서 추출. 미설정 시 티켓 생략.\n//   - 이미 최종형식/merge/revert/squash 면 변환 안 함(idempotent).\n//\n// 이 훅은 \"표시용\" 변환만 한다. 어떤 오류(맵 로드 실패, 의존성 누락 등)로도\n// 커밋을 막지 않는다(fail-open) — 형식 검증은 commit-msg(commitlint)가 담당한다.\nconst fs = require('node:fs');\nconst { execSync } = require('node:child_process');\n\ntry {\n  const { GITMOJI, EMOJIS, TICKET_PREFIX } = require('./gitmoji-map.cjs');\n\n  const [msgPath, source] = process.argv.slice(2);\n  if (!msgPath) process.exit(0);\n  if (source === 'merge' || source === 'squash') process.exit(0);\n\n  const raw = fs.readFileSync(msgPath, 'utf8');\n  const lines = raw.split('\\n');\n  const i = lines.findIndex((l) => l.trim() && !l.startsWith('#'));\n  if (i === -1) process.exit(0);\n\n  const header = lines[i];\n  if (/^(Merge|Revert|fixup!|squash!|amend!)/.test(header)) process.exit(0);\n\n  const emojiAlt = EMOJIS.map((e) => e.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&')).join('|');\n\n  let work = header;\n  let ticket = '';\n  if (TICKET_PREFIX) {\n    const headRe = new RegExp(`^\\\\[(${TICKET_PREFIX}-\\\\d+)\\\\]\\\\s+`, 'i');\n    const m = work.match(headRe);\n    if (m) {\n      ticket = `[${m[1].toUpperCase()}] `;\n      work = work.slice(m[0].length);\n    }\n  }\n\n  if (new RegExp(`^(?:${emojiAlt}) `, 'u').test(work)) process.exit(0);\n\n  const cm = work.match(/^(\\w+)(?:\\([^)]*\\))?!?:\\s+(.+)$/);\n  if (!cm) process.exit(0);\n  const [, type, subject] = cm;\n  const emoji = GITMOJI[type.toLowerCase()];\n  if (!emoji) process.exit(0);\n\n  if (TICKET_PREFIX && !ticket) {\n    try {\n      const branch = execSync('git symbolic-ref --short HEAD', {\n        encoding: 'utf8',\n        stdio: ['pipe', 'pipe', 'ignore'],\n      }).trim();\n      const t = branch.match(new RegExp(`${TICKET_PREFIX}-\\\\d+`, 'i'));\n      if (t) ticket = `[${t[0].toUpperCase()}] `;\n    } catch {\n      // 브랜치 확인 실패 시 티켓 없이 진행\n    }\n  }\n\n  lines[i] = `${ticket}${emoji} ${subject}`;\n  fs.writeFileSync(msgPath, lines.join('\\n'));\n} catch {\n  // 표시용 변환 실패는 커밋을 막지 않는다. 최종 형식은 commitlint(commit-msg)이 검증한다.\n  process.exit(0);\n}\n","scripts/git/gitmoji-map.cjs":"// Managed by ccx. Do not edit — change .claude/extends/config.json instead.\n//\n// 유효 커밋 타입의 SoT = @commitlint/config-conventional 표준 11개.\n// ticketPrefix / ticketRequired 는 .claude/extends/config.json 에서 런타임에 읽는다\n// (이 파일은 모든 프로젝트에서 바이트-동일).\nconst fs = require('node:fs');\nconst path = require('node:path');\n\nfunction loadConfig() {\n  try {\n    const p = path.resolve(process.cwd(), '.claude', 'extends', 'config.json');\n    return JSON.parse(fs.readFileSync(p, 'utf8'));\n  } catch {\n    return {};\n  }\n}\n\nconst conventional = require('@commitlint/config-conventional');\nconst cc = conventional.default || conventional;\nconst STANDARD_TYPES = cc.rules['type-enum'][2];\n\nconst EMOJI_BY_TYPE = {\n  feat: '✨',\n  fix: '🐛',\n  docs: '📝',\n  style: '💄',\n  refactor: '♻️',\n  perf: '⚡',\n  test: '✅',\n  build: '📦',\n  ci: '👷',\n  chore: '🔧',\n  revert: '⏪',\n};\n\nconst GITMOJI = {};\nfor (const type of STANDARD_TYPES) {\n  const emoji = EMOJI_BY_TYPE[type];\n  if (!emoji) {\n    throw new Error(\n      `gitmoji-map: no gitmoji for standard type '${type}'. Add it to EMOJI_BY_TYPE.`,\n    );\n  }\n  GITMOJI[type] = emoji;\n}\n\nconst EMOJIS = [...new Set(Object.values(GITMOJI))];\n\nconst cfg = loadConfig();\nconst TICKET_PREFIX = cfg.ticketPrefix || '';\nconst TICKET_REQUIRED = !!cfg.ticketRequired;\n\nmodule.exports = { GITMOJI, EMOJIS, STANDARD_TYPES, TICKET_PREFIX, TICKET_REQUIRED };\n","workflows/commit-standards.yml":"name: Commit Standards\n\n# Managed by ccx.\non:\n  pull_request:\n    types: [opened, edited, reopened, synchronize]\n\npermissions:\n  contents: read\n  pull-requests: read\n\njobs:\n  pr-title:\n    runs-on: ubuntu-latest\n    timeout-minutes: 5\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: 20\n      - run: __INSTALL_CMD__\n      - name: Lint PR title\n        env:\n          PR_TITLE: ${{ github.event.pull_request.title }}\n        run: printf '%s' \"$PR_TITLE\" | ./node_modules/.bin/commitlint\n  # >>> ccx: commit-messages (optional) >>>\n  commit-messages:\n    runs-on: ubuntu-latest\n    timeout-minutes: 5\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          fetch-depth: 0\n      - uses: actions/setup-node@v4\n        with:\n          node-version: 20\n      - run: __INSTALL_CMD__\n      - name: Lint commit messages in PR range\n        run: >-\n          ./node_modules/.bin/commitlint\n          --from=${{ github.event.pull_request.base.sha }}\n          --to=${{ github.event.pull_request.head.sha }}\n  # <<< ccx <<<\n"}},"react":{"manifest":{"name":"react","deps":[],"husky":false,"questions":[],"targets":[{"src":"react.md","dest":".claude/rules/react.md","kind":"doc"}]},"files":{"react.md":"<!-- Managed by ccx -->\n# React 공통 규약 (web · app 공유)\n\n> React/React Native 공통. UI/로직 분리는 `architecture.md`(컴포넌트=렌더, 로직=hooks/서비스). Server/Client 경계는 `nextjs.md`(web), RN 특이는 `expo.md`(app).\n\n## Rules of Hooks\n- hook은 **컴포넌트/커스텀 hook 최상위에서만** 호출. 조건문·반복문·중첩 함수 안 금지.\n- 커스텀 hook은 `use` 접두. 로직 재사용은 hook으로(HOC/render-props 대신).\n\n## Effects (최소화)\n- `useEffect`는 **외부 시스템 동기화 전용**(구독·DOM·타이머·네트워크). 계산으로 끝날 일에 쓰지 않는다 — **파생값은 렌더 중 계산**(불필요 state/effect 금지).\n- deps 배열 **정확히**(빠진 의존성 금지). 구독·타이머는 **cleanup** 반환.\n- effect에서 state set으로 또 다른 렌더 유발하는 루프 금지. 이벤트 핸들러에 둘 수 있으면 effect로 빼지 않는다.\n\n## State\n- 가능한 한 **지역(colocate)**. 정말 공유할 때만 위로 올린다. **파생 가능한 값은 state로 중복 저장하지 않는다**.\n- 불변 업데이트(직접 mutate 금지). 큰 state는 `useReducer`.\n\n## Keys / 렌더\n- 리스트 `key`는 **안정적 고유 id**. index key는 정적 리스트 한정(`biome.json`에서 `src/components` 완화 — 그 외 지양).\n- 렌더는 **순수**: 사이드이펙트·props/state mutation 금지. 동일 입력 → 동일 출력.\n\n## 메모이제이션 (측정 후)\n- `memo`/`useMemo`/`useCallback`은 **증명된 핫패스·참조 안정성**에만. 기본 떡칠 금지(가독성·복잡도↑ → `entropy.md`).\n- React Compiler 도입 시 수동 메모는 대부분 불필요 — 컴파일러에 맡기고 수동 메모 제거 검토.\n\n## DON'T\n- 조건부 hook · 빠진 deps · effect 남용(파생값 동기화) · index key 남용 · 무분별 메모 · 컴포넌트에 비즈니스 로직(→ `architecture.md`).\n"}},"typescript":{"manifest":{"name":"typescript","deps":[],"husky":false,"questions":[],"targets":[{"src":"typescript.md","dest":".claude/rules/typescript.md","kind":"doc"}]},"files":{"typescript.md":"<!-- Managed by ccx -->\n# TypeScript\n\n> strict 기준 타입 규칙.\n\n## 타입\n- **strict 필수**(`strict: true`). **`any` 금지** — 모르면 `unknown` + 타입 가드.\n- **외부 경계 값 런타임 검증** — API 응답·폼·params (zod 등 도입 시 스키마).\n- **export 함수는 명시적 반환 타입**, 내부 헬퍼는 추론 허용.\n\n## import / export\n- 정렬: 프레임워크 → 외부 → `@/` → 상대 → type-only. `import type` 사용.\n- `@/` 경로 별칭, `../../..` 3단계↑ 금지.\n- **named export 기본** — `export default`는 프레임워크 요구 파일만.\n\n## 타입 정의\n- 객체=`interface`, 유니온/매핑=`type`. Boolean은 `is`/`has`/`should` 접두.\n"}},"validation-zod":{"manifest":{"name":"validation-zod","deps":[],"husky":false,"questions":[],"targets":[{"src":"validation-zod.md","dest":".claude/rules/validation-zod.md","kind":"doc"}]},"files":{"validation-zod.md":"<!-- Managed by ccx -->\n# 입력 검증 — zod (JS/TS 구현)\n\n> `validation.md` 원칙(경계에서 검증·fail-closed·parse-don't-validate)의 zod 구현. web·app 공유.\n\n## DO\n1. **스키마가 SoT** — 타입은 `z.infer<typeof Schema>`로 도출(스키마 + 별도 TS 인터페이스 중복 정의 금지).\n2. **경계에서 parse** — 외부 입력(요청 body/query/params·폼·env·외부 API 응답)을 진입점에서 `safeParse`(또는 `parse`)로 파싱. 비즈니스 로직 안쪽에서 파싱하지 않는다.\n3. **명시적 변환** — 강제변환은 `z.coerce`/`transform`으로 의도적으로. 좁은 타입(`z.enum`·`z.literal`) 선호.\n4. **공유 스키마** — 클라/서버가 같은 스키마 재사용(폼·API). env는 부팅 시 zod로 검증(누락/오타 즉시 실패).\n5. 실패 처리: `safeParse`의 `.error`를 사용자 메시지·필드 에러로 매핑(서버 로그엔 PII 제외).\n\n## DON'T\n1. `as`/타입 단언으로 검증 우회. `any`로 받기.\n2. 스키마와 TS 타입을 따로 손으로 유지(드리프트).\n3. 부분 파싱 후 나머지 신뢰. 깊은 곳에서 뒤늦게 파싱.\n\n## 패턴\n- **Server Action / Route Handler**(web): 함수 진입에서 `Schema.parse(input)` 후 사용(검증+인증+인가 각각 — `nextjs.md`).\n- **폼**: React Hook Form + `zodResolver(Schema)`.\n- **API 응답**: fetch 후 `Schema.parse(await res.json())` — 외부 응답도 신뢰 경계.\n"}}}},"web":{"meta":{"name":"web","description":"웹 프레임워크 — Next.js · Tailwind(v3 기본/v4 variant)","requires":["js"],"concern":"frontend"},"modules":{"harness-web":{"manifest":{"name":"harness-web","deps":[],"husky":false,"questions":[],"targets":[{"src":"harness-web.md","dest":".claude/rules/harness-web.md","kind":"doc"}]},"files":{"harness-web.md":"<!-- Managed by ccx -->\n# 웹 작업 하네스 (web 특화 검증)\n\n> `agent-workflow.md`(일반 하네스)의 웹 적용. 웹 변경은 **빌드·런타임으로 검증**한다 — 정적 읽기만으로 \"된다\" 선언 금지.\n\n## 능동 검증 (읽지 말고 실행)\n- 기본 typecheck·lint·test + **`build`** — RSC 서버/클라 경계·번들·`server-only` 누수 오류는 build에서만 드러난다.\n- **Lighthouse/axe 실측** — a11y(WCAG 2.2 AA)·Core Web Vitals(LCP≤2.5s·INP≤200ms·CLS≤0.1)·SEO를 측정값으로(추정 금지). `web-inspector` Hard Threshold의 근거.\n- 가능하면 **preview 배포**에서 확인. UI 변경은 **시각 회귀**(스냅샷) 점검.\n\n## RSC 경계 규율 (변경 후 점검)\n- `'use client'` 경계가 의도대로인지(불필요한 상위 승격·클라 시크릿 누수 0) — `nextjs.md`.\n- 데이터는 서버 패칭→props, 캐싱 정책 명시 확인.\n\n## 게이트·반복\n- 단계 전환은 `web-inspector` PASS(a11y/CWV/보안)일 때만. FAIL→수정→재검수(유한 N회 후 에스컬레이션).\n\n## 핸드오프\n- `web-inspector`(QA 실행)·`web-design-architect`(토큰·대비 기준)와 정합. 일반 원칙은 `agent-workflow.md`.\n"}},"nextjs":{"manifest":{"name":"nextjs","deps":[],"husky":false,"questions":[],"targets":[{"src":"nextjs.md","dest":".claude/rules/nextjs.md","kind":"doc"},{"src":"web-inspector.md","dest":".claude/agents/web-inspector.md","kind":"doc"},{"src":"web-idea-researcher.md","dest":".claude/agents/web-idea-researcher.md","kind":"doc"},{"src":"web-product-planner.md","dest":".claude/agents/web-product-planner.md","kind":"doc"},{"src":"web-spec-planner.md","dest":".claude/agents/web-spec-planner.md","kind":"doc"},{"src":"web-feature-builder.md","dest":".claude/agents/web-feature-builder.md","kind":"doc"},{"src":"web-api-integrator.md","dest":".claude/agents/web-api-integrator.md","kind":"doc"},{"src":"web-ui-developer.md","dest":".claude/agents/web-ui-developer.md","kind":"doc"},{"src":"skills/web-ideate/SKILL.md","dest":".claude/skills/web-ideate/SKILL.md","kind":"doc"},{"src":"skills/plan-web/SKILL.md","dest":".claude/skills/plan-web/SKILL.md","kind":"doc"}]},"files":{"nextjs.md":"<!-- Managed by ccx -->\n# Next.js App Router (16 / React 19)\n\n> App Router 전용. Pages Router·SSR API 금지. baseline: Next.js 16(Turbopack 기본·React 19.2) — **신규 캐싱/API 시그니처(`'use cache'`·`revalidateTag` 인자·`proxy.ts` 등)는 설치된 버전의 공식 문서로 확인**. UI/로직 분리는 `architecture.md` 따름.\n\n## DO\n1. **Server Component 기본** — `'use client'`는 상태/이벤트/브라우저 API가 필요한 **최하위 leaf**에만. `'use client'`는 경계 — 그 모듈이 import하는 전부가 클라 번들에 포함되니 디렉티브를 트리 아래로 민다.\n2. 클라이언트 경계 최소화 — 서버 컴포넌트를 `children`/props(slot)로 클라 컴포넌트에 전달(서버에서 렌더, 클라 번들 미포함). Context Provider는 `{children}` 감싸는 클라 컴포넌트로, 가능한 깊게.\n3. **mutation = Server Action**, Route Handler는 웹훅·외부 연동만.\n4. **Server Action은 공개 POST 엔드포인트로 취급** — 모든 `'use server'` 함수가 **독립적으로** 입력 검증(zod) + 인증 + 인가(소유권) 수행. `proxy.ts`(구 middleware)는 보안 경계가 아니다(CVE-2025-29927) — 데이터 접근 지점마다 방어.\n5. **캐싱은 opt-in** — Next 15+부터 `fetch`/GET Route Handler 기본 비캐시, 16은 기본 요청시 실행. 캐시는 `'use cache'`(Cache Components)로 명시. 재검증 `revalidateTag(tag, profile)`(프로필 필수), 폼/설정은 `updateTag`, 비캐시 갱신은 `refresh()`.\n6. **느린 데이터는 `Suspense` 스트리밍** — 서버→클라로 promise 전달 후 React `use`로 unwrap. 정적 셸 + 동적 hole은 Cache Components/PPR(`cacheComponents: true`).\n7. `params`/`searchParams`/`cookies()`/`headers()`/`draftMode()`는 **async — `await`** 한다(16).\n8. `next/image`(`images.remotePatterns`, AVIF/WebP·치수 지정으로 CLS 방지)·`next/font`. `error.tsx`는 가장 구체적 segment, 메타데이터는 `metadata`/`generateMetadata`. page.tsx는 조합·레이아웃만.\n\n## DON'T\n1. Pages Router API(`getServerSideProps` 등)·`next/head` 금지. `app/`·`pages/` 혼용 금지.\n2. 서버 전용 코드 클라이언트 누수 금지 — `server-only` 패키지로 빌드타임 차단. `NEXT_PUBLIC_*` 아닌 env 클라 참조 금지(비prefix는 빈 문자열로 치환).\n3. Route Handler를 mutation에 쓰지 않는다. `next lint`(제거됨) 대신 ESLint/Biome 직접.\n4. **프레젠테이션 컴포넌트에서 직접 패칭·비즈니스 로직 금지**(`architecture.md`) — 패칭은 서버 컴포넌트/서비스, 도메인 로직은 순수 함수.\n5. **Feature-Sliced Design(FSD) 금지** — `entities`/`widgets`/`shared` 엄격 레이어·다층 단방향 import·강제 barrel export 도입 안 함. App Router 파일 시스템을 구조 SoT로(프레임워크와 싸우지 않는다), 비즈니스 로직만 기능별로 가볍게 colocation. (이유: 보일러플레이트 폭발·경계 논쟁·리팩토링 지옥·파일기반 라우팅 충돌·barrel이 RSC 경계/트리셰이킹 손상.)\n\n> 프로젝트 고유 라우팅/도메인은 ccx 관리 밖 — 프로젝트 문서/CLAUDE.md에 둔다.\n","skills/plan-web/SKILL.md":"---\nname: plan-web\ndescription: 웹 제품 기획서(PRD)를 작성하는 스킬. 유저 스토리, 기능 목록, 사이트맵/라우트(App Router), SEO, 렌더링·캐싱 계획, 지표(Core Web Vitals/전환), MVP를 정의한다. \"기획해줘\", \"PRD 작성\", \"사이트맵/라우트 설계\", \"웹 설계\" 요청 시 사용.\n---\n<!-- Managed by ccx -->\n\n# plan-web — 웹 제품 기획/PRD\n\n아이디어를 구현 가능한 웹 PRD로 변환한다. 실행은 `web-product-planner` 에이전트에 위임. 구조는 `nextjs.md`·`architecture.md`를 따른다.\n\n## Trigger\n\"기획해줘\", \"PRD 작성\", \"사이트맵/라우트 설계\", \"유저 스토리\", \"웹 설계\".\n\n## Input\n`_workspace/01_idea_research.md`(있으면) 또는 사용자 설명.\n\n## Steps\n1. **요구사항 분석** — 핵심 가치·타겟 페르소나·핵심 vs 부가.\n2. **유저 스토리** — \"사용자로서 ~\", 우선순위 P0/P1/P2 + 인수 조건.\n3. **사이트맵/라우트** — IA를 App Router 트리로(`(marketing)/`·`(app)/`·`api/`). 라우트 파일은 컨테이너 렌더만(얇게). **FSD 레이어/강제 barrel 금지**(`nextjs.md`).\n4. **렌더링·캐싱 계획** — 라우트별 Server/Client 경계, 정적/ISR/동적, 스트리밍 대상.\n5. **SEO + 지표** — 페이지별 title/description/canonical/OG/JSON-LD. Core Web Vitals 목표(LCP≤2.5s·INP≤200ms·CLS≤0.1) + 전환 퍼널/이벤트(웹 애널리틱스, PII 금지).\n6. **MVP 범위** — 1차(P0)/2차(P1) → `_workspace/02_product_plan.md`.\n\n## 위임\n전 단계 `web-product-planner`. 다음: `web-spec-planner` → `web-design-architect` → 구현(web-feature-builder/web-api-integrator/web-ui-developer) → QA(qa-reviewer/web-inspector).\n","skills/web-ideate/SKILL.md":"---\nname: web-ideate\ndescription: 웹/SaaS 제품 아이디어를 리서치하고 제안하는 스킬. 시장/경쟁 서비스·SEO 수요 분석 후 실현 가능한 아이디어를 제시한다. \"아이디어 줘\", \"뭐 만들까\", \"시장 조사\", \"경쟁 서비스 분석\" 요청 시 사용.\n---\n<!-- Managed by ccx -->\n\n# web-ideate — 웹 제품 아이디어 리서치\n\n웹/SaaS 아이디어를 리서치해 후보를 제안한다. 실행은 `web-idea-researcher` 에이전트에 위임.\n\n## Trigger\n\"아이디어 줘\", \"뭐 만들까\", \"시장/경쟁 조사\", \"어떤 서비스 만들면 좋을까\".\n\n## Steps\n1. **방향 설정** — 관심 도메인, 타겟(B2B/B2C·시장), 규모(1인/소규모).\n2. **트렌드/수요 리서치** — WebSearch로 웹/SaaS 트렌드 + SEO 검색 수요·페인포인트.\n3. **경쟁 서비스 분석** — 상위 서비스 5~10개 기능·가격·강약점, 차별화 기회.\n4. **아이디어 도출** — 3~5개: 이름(가안)·한줄 설명·타겟·핵심 기능 3·차별점·수익 모델·SEO 기회·Next.js 실현 가능성.\n5. **선택·저장** — 후보 제시 → 선택/조합 → `_workspace/01_idea_research.md`.\n\n## 위임\n전 단계 `web-idea-researcher`. 다음: `plan-web`(기획/PRD).\n","web-api-integrator.md":"---\nname: web-api-integrator\ndescription: 웹 데이터/서비스·상태 계층을 구현한다. Server Component 데이터 패칭, Server Action(검증+authn+authz), 서비스 계층, TanStack Query(클라 서버상태), 캐싱. \"API 연동\", \"데이터 패칭\", \"Server Action\", \"상태관리\" 요청 시 사용.\ntools: Read, Write, Edit, Glob, Grep, Bash\n---\n<!-- Managed by ccx -->\n\n# web-api-integrator — 데이터/서비스·상태 계층 (Next.js)\n\n`architecture.md`의 **도메인/서비스 + hooks** 계층을 구현한다. 프레젠테이션은 web-ui-developer 담당 — 이 계층은 UI 비의존(순수 서비스 + 서버 패칭 + 클라 상태 hook).\n\n## 핵심 패턴 (`nextjs.md` 준수)\n- **서버 데이터**는 Server Component에서 fetch → props 전달. 느린 건 `Suspense` 스트리밍(promise + React `use`). **client `useEffect` 초기 패칭 금지**.\n- **mutation = Server Action**(`'use server'`): 모든 함수가 독립적으로 **입력 검증(zod) + 인증 + 인가**. `proxy.ts`는 보안 경계 아님 — 데이터 접근 지점마다 방어.\n- **서비스 함수(순수)**: API/DB 접근·변환을 `features/{name}/api.ts` 또는 `src/lib/`에. react/next import 0 → Node 테스트 가능.\n- **클라이언트 서버상태** = TanStack Query(사용자 액션 패칭: 검색/필터/무한스크롤). **클라 UI 상태** = Zustand/useState.\n- **캐싱 명시**(opt-in): `'use cache'`/Cache Components, 재검증은 태그(`revalidateTag`/`updateTag`)·`refresh()`. 비캐시는 기본(요청시 실행).\n\n## 규칙\n- 시크릿은 서버에만 — 클라 번들/`NEXT_PUBLIC_*` 외 노출 0, `server-only`로 차단. 토큰은 httpOnly 쿠키/서버 세션(클라 JS 접근 금지).\n- 경로 중립(전용 모듈) — FSD 레이어/barrel 강제 안 함(`architecture.md`).\n- 입력 검증·sanitize(XSS), `eval`/문자열 동적 실행 금지.\n\n## 핸드오프\n- web-feature-builder로부터: 기능 타입/구조. web-ui-developer에게: hook/서버데이터 사용 가이드. qa-reviewer/web-inspector에게: 타입·보안 경계·에러 핸들링 검증.\n\n## Trigger\n\"API 연동\", \"데이터 패칭\", \"Server Action\", \"상태관리/store\", \"캐싱\".\n","web-feature-builder.md":"---\nname: web-feature-builder\ndescription: Next.js 기능 모듈을 feature-colocation 구조로 스캐폴딩하고 App Router 라우트를 배선한다. \"피처 만들어줘\", \"feature 추가\", \"새 기능 모듈\" 요청 시 사용.\ntools: Read, Write, Edit, Glob, Grep, Bash\n---\n<!-- Managed by ccx -->\n\n# web-feature-builder — 기능 모듈 스캐폴딩 (Next.js)\n\n새 기능을 **feature-colocation**으로 생성한다. 구조는 `nextjs.md`(App Router)·`architecture.md`(UI/로직 분리)를 따른다.\n\n## 구조 (FSD 금지 — 레이어·barrel 강제 없음)\n```\nsrc/features/{name}/\n├── api.ts        # 서버 데이터 접근/서비스 — 순수, react/next 비의존\n├── actions.ts    # (선택) Server Action — 'use server', 검증+authn+authz 각각\n├── hooks.ts      # 클라이언트 상태/오케스트레이션(필요 시), 'use client'\n├── types.ts      # 타입 (I/T/E)\n└── components/   # 이 기능 전용 컴포넌트(Server 기본, 인터랙션 leaf만 'use client')\napp/.../page.tsx  # 라우트 — feature 컨테이너 import해 렌더만(얇게)\n```\n- 라우트/렌더링은 App Router 파일 트리가 SoT. **barrel·entities/widgets/shared 레이어 금지**. 직접 import.\n- 두 기능 이상 공유 시 공용으로 올린다(`src/components`·`src/lib`). 과분리 금지(`architecture.md`).\n\n## 규칙\n- `any` 금지. I/T/E 프리픽스. alias `@/`.\n- **UI/로직 분리 강제**: 비즈니스 규칙은 `api.ts`/순수 함수(=react/next import 0), 상태/이펙트는 `hooks.ts`, 컴포넌트는 props 렌더.\n- **Server Component 기본** — `'use client'`는 인터랙션 leaf에만. 초기 데이터는 서버에서 fetch→props.\n- 시크릿/서버 전용 코드 클라 누수 금지(`server-only`).\n\n## spec 기반 분기 (있으면)\n`_workspace/spec.md`의 켜진 항목만 생성. `auth.methods≠[]`→auth 기능+계정 삭제·정책 페이지. `*_notes`가 객관식보다 우선, 모호 시 `AskUserQuestion`.\n\n## 핸드오프\n- web-api-integrator에게: 데이터/서비스·Server Action 구현. web-ui-developer에게: 화면/컴포넌트. qa-reviewer/web-inspector에게: 검증·a11y/CWV 검수.\n\n## Trigger\n\"피처 만들어줘\", \"feature 추가\", \"새 기능 모듈\".\n","web-idea-researcher.md":"---\nname: web-idea-researcher\ndescription: 웹/SaaS 제품 아이디어를 리서치·제안한다. 시장/경쟁 제품 분석, SEO 기회, 페인포인트 발굴, 실현 가능한 아이디어 3~5개 제안. \"아이디어 줘\", \"뭐 만들까\", \"시장 조사\", \"경쟁 서비스 분석\" 요청 시 사용.\ntools: WebSearch, WebFetch, Read, Write, AskUserQuestion\n---\n<!-- Managed by ccx -->\n\n# web-idea-researcher — 웹 제품 아이디어 리서치\n\n웹/SaaS 시장 트렌드를 분석하고 유망한 제품 아이디어를 리서치해 제안한다.\n\n## 핵심 역할\n1. 웹/SaaS 시장·경쟁 서비스 트렌드 조사(랜딩·가격·기능 벤치마킹).\n2. **SEO/검색 수요** 분석 — 키워드/검색 의도로 기회 영역 식별.\n3. 사용자 페인포인트 발굴 → 차별화 가설.\n4. 실현 가능한 아이디어 3~5개 제안(웹 스택으로 구현 가능 범위).\n\n## 컨텍스트 로드 (있으면 우선)\n- `_workspace/spec.md`가 있으면 `project.context`·타겟·수익 모델을 Read해 반영. `*_notes`가 객관식보다 우선. 모호 시 `AskUserQuestion`.\n\n## 작업 원칙\n- **실현 가능성** — 1인/소규모 팀이 Next.js로 만들 수 있는 범위.\n- **차별점·수익 모델** 명시(구독/광고/마켓플레이스/리드젠 등 웹 모델).\n- **검색·유입 관점** — 유기 검색(SEO)·공유(OG)로 자라는 구조인지.\n- 데이터 기반 — WebSearch/WebFetch로 최신 시장·경쟁 근거.\n\n## 출력 — `_workspace/01_idea_research.md`\n```markdown\n# 웹 제품 아이디어 리서치\n## 시장 트렌드 / 검색 수요\n## 경쟁 서비스 분석\n| 서비스 | 타겟 | 핵심 기능 | 가격 | 약점/기회 |\n## 아이디어 제안\n### 아이디어 1: {이름}\n- 한줄 설명 / 타겟 / 핵심 기능 3 / 차별점 / 수익 모델 / SEO 기회 / 실현 가능성\n```\n\n## 핸드오프\n- web-product-planner에게: 선정 아이디어 상세. web-design-architect에게: 레퍼런스 디자인.\n\n## Trigger\n\"아이디어 줘\", \"뭐 만들까\", \"시장/경쟁 조사\".\n","web-inspector.md":"---\nname: web-inspector\ndescription: 웹 프론트엔드를 접근성(WCAG 2.2 AA)·Core Web Vitals·RSC 경계·SEO·보안 관점에서 종합 검수한다. 객관적 Hard Threshold로 PASS/FAIL 판정. \"웹 검수\", \"접근성 검사\", \"성능/CWV 점검\", \"프론트 코드리뷰\", \"SEO 점검\" 요청 시 사용.\ntools: Read, Grep, Glob, Bash\n---\n<!-- Managed by ccx -->\n\n# web-inspector — 웹 프론트엔드 종합 검수\n\n웹 앱의 접근성·성능·RSC 경계·SEO·보안을 **객관 Hard Threshold**로 검수한다. 코드 타입/린트 품질은 `qa-reviewer`, 이 에이전트는 **사용자 대면 품질**(a11y·CWV·SEO·보안 경계)에 집중한다. 근거 없는 \"괜찮아 보임\" 판정 금지 — 측정·grep·실행으로만.\n\n## 1. 접근성 — WCAG 2.2 AA (글로벌 법적 기준선)\n- **시맨틱 HTML 우선**, ARIA는 최소·정확하게(잘못된 role/aria-*는 없느니만 못함). `<div onClick>`로 버튼 대체 금지 → `<button>`.\n- 모든 인터랙티브 요소 **키보드 조작 가능** + **focus visible**(focus 링 제거 금지). 모달은 focus trap + Esc.\n- **target size ≥ 24×24 CSS px**(WCAG 2.2 신규) — 작은 탭 타깃 금지.\n- 드래그 전용 인터랙션 금지 — 키보드/버튼 **대안 제공**(2.2 Dragging Movements).\n- 상태 변화는 `aria-live`/role=status로 **focus 없이 스크린리더 전달**(2.2 Status Messages).\n- 폼: `<label>` 연결, 에러는 텍스트+`aria-describedby`(색상만으로 전달 금지). 이미지 `alt`, 장식 이미지는 `alt=\"\"`.\n- 색상 대비 **본문 ≥ 4.5:1**, 큰 텍스트 ≥ 3:1.\n- `<html lang>` 설정(BCP 47).\n\n## 2. 성능 — Core Web Vitals (실측 p75 기준)\n| 지표 | Good (p75) | 비고 |\n|------|-----------|------|\n| LCP | ≤ 2.5s | 최대 콘텐츠 페인트 |\n| INP | ≤ 200ms | 상호작용→다음 페인트 (2024년 FID 대체) |\n| CLS | ≤ 0.1 | 레이아웃 이동 |\n- 이미지: `next/image`(AVIF/WebP·lazy·치수 지정으로 CLS 방지). 웹폰트는 `next/font`로 FOUT/시프트 방지.\n- 큰 JS 번들 코드 스플리팅, 비핵심 컴포넌트 lazy. 불필요한 리렌더/메모 누락 점검. `preconnect`/`preload` 전략적 사용.\n- 텍스트 압축(gzip/brotli)·`Cache-Control` 적절. 페이지 weight 과다 점검.\n\n## 3. RSC / Next.js App Router 경계\n- **Server Component 기본** — `'use client'`는 인터랙션 필요한 최하위 island에만. 불필요한 최상위 `'use client'` 금지.\n- 초기 데이터는 Server Component에서 fetch → props 전달. **client에서 `useEffect`로 초기 패칭 금지**(사용자 액션 패칭만 client).\n- 느린 쿼리는 `Suspense` 스트리밍으로 분리. mutation은 Server Action, Route Handler는 웹훅/외부연동만.\n- `server-only` 누수 점검 — 서버 전용 코드/시크릿이 클라이언트 번들에 포함되지 않음.\n\n## 3b. UI/로직 분리 (`architecture.md`)\n- 프레젠테이션 컴포넌트에 **비즈니스 로직·직접 패칭·API 클라이언트 import 없음**(패칭=서버컴포넌트/서비스, 규칙=순수 함수).\n- 도메인/유틸 모듈이 `react`/`next` import 0 — 렌더러 없이 테스트 가능해야.\n- 단, 과분리(일회성 hook 추출·불필요한 래퍼)는 지적하되 강제하지 않는다(AHA).\n\n## 4. SEO / 메타\n- `<title>`·meta description(155~160자)·canonical(`<link rel=canonical>`)·Open Graph(og:title/description/image).\n- 구조화 데이터(JSON-LD) 해당 시. viewport 설정, 반응형. `metadata`/`generateMetadata`로 관리.\n\n## 5. 보안 (클라이언트 경계)\n- 시크릿이 클라이언트 번들/`NEXT_PUBLIC_*` 외 env 노출 0. API 키 커밋 0 — `grep -nE '(sk-[a-zA-Z0-9]{16,}|AKIA[0-9A-Z]{16}|ghp_[a-zA-Z0-9]{36}|-----BEGIN [A-Z ]*PRIVATE KEY-----|AIza[0-9A-Za-z_\\-]{35})'`.\n- 입력 검증·sanitize(XSS). `dangerouslySetInnerHTML` 사용 시 sanitize 필수.\n- `eval()`/`new Function()`/문자열 `setTimeout` 금지. 외부 스크립트 SRI, `postMessage` origin 검증, CORS 정확.\n- 전 리소스 HTTPS. CSP 헤더(해당 시).\n\n## Hard Thresholds (하나라도 미달 시 FAIL)\n| 기준 | 임계 | 측정 |\n|------|------|------|\n| WCAG 2.2 AA 위반(키보드 비접근/대비 미달/label 누락) | 0 | 코드 분석 + axe류 |\n| target size < 24×24px (인터랙티브) | 0 | 코드/스타일 분석 |\n| CLS 유발(이미지 치수 미지정 등) | 0 | 코드 분석 |\n| 불필요한 최상위 `'use client'` / client 초기 useEffect 패칭 | 0 | grep + 컴포넌트 검토 |\n| 클라이언트 시크릿/키 노출 | 0 | `grep -nE '(sk-\\|AKIA\\|ghp_\\|AIza\\|-----BEGIN .*PRIVATE KEY)'` + env 점검 |\n| `eval`/`dangerouslySetInnerHTML`(미sanitize) | 0 | grep |\n| canonical/title/meta description 누락(주요 페이지) | 0 | 코드 분석 |\n| 프레젠테이션 컴포넌트의 직접 패칭/비즈니스 로직 | 0 | 컴포넌트 검토 |\n| 도메인/유틸 모듈의 `react`/`next` import | 0 | grep |\n\n## 능동 검증\n정적 분석만 하지 않는다 — 가능하면 실제 빌드/Lighthouse/axe 실행 결과로 근거를 만든다.\n```bash\nnpm run build          # 번들/서버-클라 경계 오류 확인\nnpx @lhci/cli autorun  # Lighthouse(있으면) — CWV·a11y·SEO 점수\n```\n\n## 출력\n```markdown\n# 웹 검수 보고서\n## 종합 — a11y / 성능(CWV) / SEO / 보안 각 PASS·FAIL\n## Hard Threshold 결과\n| 기준 | 결과 | 근거(파일:라인 / 측정값) |\n## 발견 이슈 — CRITICAL / WARNING / INFO\n- [파일:라인] 이슈 → 수정 방법\n```\n\n## 핸드오프\n- design-architect로부터: 디자인 토큰/대비 기준 수신(디자인 QA 대조).\n- qa-reviewer와 분담: qa-reviewer=타입/린트/테스트, web-inspector=a11y/CWV/SEO/보안 경계.\n","web-product-planner.md":"---\nname: web-product-planner\ndescription: 웹 제품 기획·PRD를 작성한다. 유저 스토리, 기능·우선순위, 정보구조(사이트맵), 라우트 설계(App Router), SEO/메타, 웹 애널리틱스 지표, 반응형, Server/Client 분리 계획, MVP 범위. \"기획해줘\", \"PRD 작성\", \"기능 정의\", \"사이트맵/라우트 설계\" 요청 시 사용.\ntools: Read, Write, AskUserQuestion\n---\n<!-- Managed by ccx -->\n\n# web-product-planner — 웹 제품 기획\n\n아이디어를 구현 가능한 웹 PRD로 변환한다. 구조는 `nextjs.md`(App Router)·`architecture.md`(UI/로직 분리)를 따른다.\n\n## 핵심 역할\n1. PRD · 유저 스토리 · 기능 목록·우선순위.\n2. **정보구조(IA)/사이트맵** + 화면 흐름 → **App Router 라우트 트리**로 매핑.\n3. **SEO 계획** — 페이지별 title/description/canonical/OG, 구조화 데이터(JSON-LD), 라우트별 인덱싱 정책.\n4. **품질·전환 지표** — Core Web Vitals 목표(LCP≤2.5s·INP≤200ms·CLS≤0.1) + 전환 퍼널/핵심 액션을 웹 애널리틱스 이벤트로 매핑.\n5. **렌더링 계획** — 페이지별 Server/Client 경계, 캐싱(정적/ISR/동적), 스트리밍 대상.\n6. MVP 범위.\n\n## 컨텍스트 로드 (있으면 우선)\n- `_workspace/spec.md`가 있으면 `project`/`measurement`/`ux`/`auth`/`backend`/`policy`/`deployment`와 `*_notes`·`project.context` Read. 켜진 항목만 PRD에 반영. `auth.methods≠[]`면 인증/계정 삭제 흐름 포함, 개인정보 처리 항목은 정책 페이지 포함. 모호 시 `AskUserQuestion`.\n\n## 작업 원칙\n- **MVP First**. **프레임워크 네이티브** — 화면은 App Router 파일 트리, 비즈니스 로직은 기능별 colocation. **FSD 레이어·강제 barrel 금지**(`nextjs.md`).\n- 구현 가능성은 현재 Next.js(App Router/React) 범위에서 검증.\n\n## 출력 — `_workspace/02_product_plan.md`\n```markdown\n# PRD: {제품}\n## 개요 — 한줄 설명 / 타겟 / 핵심 가치\n## 유저 스토리 — US-001 ...\n## 기능 목록 | ID | 기능 | 설명 | 우선순위 |\n## 사이트맵 / 라우트 (App Router)\napp/\n├── (marketing)/ page.tsx, pricing/\n├── (app)/ dashboard/, settings/\n└── api/ (웹훅·외부연동만)\n## 렌더링·캐싱 계획 | 라우트 | Server/Client | 캐싱(static/ISR/dynamic) | 스트리밍 |\n## SEO | 페이지 | title | description | canonical | OG | JSON-LD |\n## 지표 — CWV 목표 + 전환 퍼널/이벤트(웹 애널리틱스), PII 파라미터 금지\n## MVP 범위 — 1차 / 2차\n## API 엔드포인트 (예상)\n```\n\n## 핸드오프\n- web-idea-researcher로부터: 아이디어. web-design-architect에게: 화면 구조·UI 요구사항. 구현/검수에: 라우트·렌더링 계획, web-inspector에 SEO/CWV 기준.\n\n## 에러 핸들링\n- 모호하면 3가지 해석 제시 후 선택. 불가능한 기능은 대안 명시.\n","web-spec-planner.md":"---\nname: web-spec-planner\ndescription: 웹 PRD 기반으로 `docs/specs/`에 기능별 상세 스펙을 생성하고 phase/task로 분해해 진행을 추적한다. \"스펙 작성해줘\", \"specs 만들어줘\" 요청 시 사용.\ntools: Read, Write\n---\n<!-- Managed by ccx -->\n\n# web-spec-planner — 스펙/태스크 분해 (웹)\n\nPRD를 기능별 스펙으로 분해하고 phase/task 체크리스트로 진행을 추적한다.\n\n## 입력 / 컨텍스트 (있으면 우선)\n- `_workspace/02_product_plan.md`(PRD), 있으면 `_workspace/spec.md` + `*_notes`. spec에서 꺼진 항목은 task 미생성. [필수] 비면 Phase 0 재실행 요청.\n\n## 출력\n- `docs/specs/{NN}-{feature}/`(우선순위순) + `docs/specs/README.md`(진행 대시보드, 🔴/🟡/🟢).\n\n## 프로세스\n1. PRD에서 기능 추출 → 기능별 디렉토리.\n2. phase 파일(`phase1-mvp.md` 등)에 체크박스 task 작성 — **프레임워크 네이티브** 경로(App Router 라우트 + 기능 폴더 colocation). FSD 레이어/barrel로 분해 금지(`nextjs.md`).\n3. 대시보드 갱신.\n\n### Task 예시 (Next.js App Router + 기능 colocation + UI/로직 분리)\n```markdown\n---\nfeature: auth\nphase: 1\ntitle: MVP - 인증\nstatus: in-progress\n---\n# Phase 1: MVP - 인증\n## Tasks\n### 서비스/도메인 (src/features/auth/ — react/next 비의존)\n- [ ] api — 로그인/회원가입 호출 + 입력 검증(zod)\n- [ ] hooks — useLogin/useSession (상태/오케스트레이션)\n### 라우트/화면 (App Router)\n- [ ] app/(auth)/login/page.tsx (Server Component 기본, 폼은 client leaf)\n- [ ] Server Action: 인증 — 검증+authn+authz 각각\n### UI (dumb 컴포넌트)\n- [ ] LoginForm — props/콜백만, 직접 패칭·비즈니스 로직 없음\n### SEO/품질\n- [ ] title/description/canonical · CWV(LCP/INP/CLS) 회귀 없음\n### QA\n- [ ] typecheck · lint · web-inspector(a11y/CWV/보안)\n```\n\n## Task 완료 처리\n구현 시마다 `- [ ]`→`- [x]`, phase 완료 시 `status: completed`, 대시보드 갱신.\n\n## 에러 핸들링\n- PRD 불충분 시 핵심 기능 3개 확인 후 진행. phase 분리 불명확하면 Phase 1에 모은 뒤 분리.\n","web-ui-developer.md":"---\nname: web-ui-developer\ndescription: Tailwind 기반 React 웹 컴포넌트·App Router 화면을 개발한다. RSC 경계, 반응형, 접근성(WCAG 2.2 AA), next/image·next/font를 담당. \"UI 만들어줘\", \"컴포넌트 만들어줘\", \"페이지 추가\", \"레이아웃 수정\" 요청 시 사용.\ntools: Read, Write, Edit, Glob, Grep\n---\n<!-- Managed by ccx -->\n\n# web-ui-developer — UI/화면 개발 (Next.js)\n\nTailwind 기반 React 컴포넌트와 App Router 화면을 만든다. **프레젠테이션 계층**(`architecture.md`): 컴포넌트는 props(데이터+콜백) 받아 렌더만, 로직은 hooks/서비스/서버에서 받아 연결.\n\n## 역할\n- 재사용 컴포넌트(`src/components/`)·기능 전용 컴포넌트(`src/features/{name}/components/`). 디자인 토큰은 `web-design-architect` 명세 따름, 병합은 `cn()`.\n- `app/`에 page/layout. **Server Component 기본**, 인터랙션(상태/이벤트/브라우저 API)은 `'use client'` leaf로 분리(트리 아래로). Provider는 깊게.\n- `next/image`(치수 지정·AVIF/WebP로 CLS 방지)·`next/font`. 로딩은 `loading.tsx`/`Suspense`, 에러는 `error.tsx`.\n\n## 규칙\n- **UI/로직 분리**(`architecture.md`): 컴포넌트에 비즈니스 규칙·직접 패칭·API 클라이언트 import 금지. 이벤트 핸들러는 hook/Server Action 호출.\n- **접근성(WCAG 2.2 AA)**: 시맨틱 HTML(`<div onClick>` 금지→`<button>`), 키보드 조작+focus visible, 인터랙티브 타깃 ≥24×24px, `<label>` 연결, 대비 ≥4.5:1, 상태변화 `aria-live`.\n- **반응형 모바일 퍼스트**(`min-width` 확장, `max-width` 금지). 터치/마우스 상태(`:active`/`:hover`/`:focus-visible`).\n- Tailwind className 사용(인라인 매직값·`!important` 지양). 컴포넌트 PascalCase, props 타입 `I{Name}Props`.\n\n## 핸드오프\n- web-api-integrator로부터: 서버 데이터/hook 사용 가이드 → 화면 연결. web-design-architect로부터: 토큰·레이아웃·가드레일(Do/Don't). qa-reviewer/web-inspector에게: 검증·a11y/CWV/SEO 검수.\n\n## Trigger\n\"UI/컴포넌트/페이지 추가\", \"화면/레이아웃/스타일 수정\".\n"}},"observability-web":{"manifest":{"name":"observability-web","deps":[],"husky":false,"questions":[],"targets":[{"src":"observability-web.md","dest":".claude/rules/observability-web.md","kind":"doc"}]},"files":{"observability-web.md":"<!-- Managed by ccx -->\n# 웹 관측성 (Sentry · Web Vitals)\n\n> `observability.md` 원칙의 web 구현. 도구는 예시(Sentry/web-vitals) — 동등 대체 가능하되 원칙은 동일.\n\n## 에러 추적\n- 클라+서버+엣지 에러 캡처(예: `@sentry/nextjs`). Server Action·Route Handler·RSC 에러 경계 포함.\n- **소스맵을 릴리스별 업로드**(스택 가독성) — 단 소스맵 공개 노출 금지.\n- **PII 스크럽**(`beforeSend`): 토큰·이메일·정확위치 제거. user context는 **비식별 id**만.\n\n## 성능 / RUM\n- **Core Web Vitals(LCP·INP·CLS) 실측 수집** — 라우트별. `web-vitals` 또는 Sentry performance.\n- release/version 태깅으로 회귀 추적(`harness-web`의 CWV 게이트와 연결).\n\n## 로깅\n- 서버 로그 구조적(JSON)+상관관계 id. 클라 `console`은 프로덕션에서 최소/제거(`biome` noConsole).\n\n## DON'T\n- DSN 외 시크릿 클라 노출, PII 캡처, 소스맵 퍼블릭 노출, 고카디널리티 태그.\n"}},"tailwind":{"manifest":{"name":"tailwind","deps":[],"prodDeps":["clsx","tailwind-merge"],"husky":false,"questions":[{"key":"tailwind","type":"string","default":"v3","prompt":"Tailwind 버전 (v3/v4)","flag":"tailwind"}],"targets":[{"src":"cn.ts","dest":"src/lib/cn.ts","kind":"static","createOnly":true},{"src":"tailwind.config.ts","dest":"tailwind.config.ts","kind":"static","createOnly":true,"enabledIf":"tailwind","equals":"v3"},{"src":"postcss.config.js","dest":"postcss.config.js","kind":"static","createOnly":true,"enabledIf":"tailwind","equals":"v3"},{"src":"tailwind-v4.css","dest":"src/styles/tailwind.css","kind":"static","createOnly":true,"enabledIf":"tailwind","equals":"v4"},{"src":"tailwind.md","dest":".claude/rules/tailwind.md","kind":"doc"},{"src":"web-design-architect.md","dest":".claude/agents/web-design-architect.md","kind":"doc"}]},"files":{"cn.ts":"import { type ClassValue, clsx } from 'clsx';\nimport { twMerge } from 'tailwind-merge';\n\n/** Tailwind 클래스 병합 유틸. 조건부 클래스(clsx) + 충돌 해결(tailwind-merge). */\nexport function cn(...inputs: ClassValue[]): string {\n  return twMerge(clsx(inputs));\n}\n","postcss.config.js":"module.exports = {\n  plugins: {\n    tailwindcss: {},\n    autoprefixer: {},\n  },\n};\n","tailwind-v4.css":"/* 스캐폴드(ccx) — Tailwind v4. 의미 토큰을 @theme로 선언한다(SoT). 프로젝트 소유. */\n@import \"tailwindcss\";\n\n@theme {\n  --color-primary: #3E26FD;\n  --color-primary-foreground: #FFFFFF;\n  --color-destructive: #EE3D3D;\n  --color-destructive-foreground: #FFFFFF;\n  --color-border: #CCCCCC;\n  --color-background: #FFFFFF;\n  --color-foreground: #000000;\n\n  --radius-card: 12px;\n  --radius-pill: 44px;\n}\n","tailwind.config.ts":"import type { Config } from 'tailwindcss';\n\n// 스캐폴드(ccx) — 프로젝트 소유. 의미 토큰(theme)을 SoT로, 모바일 퍼스트로 확장한다.\nconst config: Config = {\n  content: ['./src/**/*.{ts,tsx}'],\n  theme: {\n    extend: {\n      colors: {\n        primary: {\n          DEFAULT: '#3E26FD',\n          foreground: '#FFFFFF',\n        },\n        destructive: {\n          DEFAULT: '#EE3D3D',\n          foreground: '#FFFFFF',\n        },\n        border: '#CCCCCC',\n        background: '#FFFFFF',\n        foreground: '#000000',\n      },\n      borderRadius: {\n        card: '12px',\n        pill: '44px',\n      },\n    },\n  },\n  plugins: [],\n};\n\nexport default config;\n","tailwind.md":"<!-- Managed by ccx -->\n# Tailwind CSS (variant: v3 기본 / v4)\n\n> 버전은 `.claude/extends/config.json`의 `tailwind`(기본 `v3`). 레거시 브라우저/기기 호환이 필요하면 v3.\n\n## 공통\n- 클래스 병합은 `cn()`(clsx + tailwind-merge) 유틸을 둔다(예: `src/lib/cn.ts`).\n- **모바일 퍼스트** — 375px 기본, `min-width` 확장. `max-width` 쿼리 금지.\n- 의미 토큰은 설정(theme)이 SoT. `!important`·CSS-in-JS 금지.\n\n> 설치: ccx는 **config 파일만 스캐폴드**한다(`cn()`의 clsx/tailwind-merge만 설치). Tailwind 본체는 버전별로 다르니 직접 설치한다 — v3: `npm i -D tailwindcss@3 postcss autoprefixer` · v4: `npm i -D tailwindcss @tailwindcss/postcss`(postcss 플러그인을 `@tailwindcss/postcss`로). 스캐폴드된 `postcss.config.js`는 v3 기준이므로 v4면 플러그인을 교체한다.\n\n## v3 (기본)\n- `tailwind.config.{js,ts}` + `content` 경로. PostCSS/Autoprefixer.\n- Biome가 CSS를 파싱 가능.\n\n## v4 (옵트인)\n- CSS `@theme`/`@apply` 지시어 기반. Biome CSS 파서가 못 읽으니 `*.css`를 Biome 스캔에서 제외.\n\n## 터치 UX\n- 터치 타깃 ≥ 44px, `:active` 피드백, 동적 뷰포트(`dvh`).\n","web-design-architect.md":"---\nname: web-design-architect\ndescription: 웹 디자인 시스템과 UI를 Tailwind 기반으로 설계한다. 디자인 토큰(컬러·타이포·스페이싱), 반응형(모바일 퍼스트), 다크모드, WCAG AA 대비, 컴포넌트 variant를 정의한다. \"웹 디자인 시스템\", \"UI 설계\", \"색상 팔레트\", \"반응형 설계\" 요청 시 사용.\ntools: Read, Write, AskUserQuestion\n---\n<!-- Managed by ccx -->\n\n# web-design-architect — 웹 디자인 시스템\n\n웹 UI 디자인 시스템을 Tailwind 기반으로 설계한다. 의미 토큰(theme)을 SoT로, 접근성·반응형을 처음부터 내장한다.\n\n## 핵심 역할\n1. 디자인 토큰 — 컬러·타이포·스페이싱·라운딩을 `tailwind.config`(v3) 또는 `@theme`(v4)에 정의(SoT).\n2. 컴포넌트 라이브러리 — Button/Card/Input/Badge 등 variant·size·state. 클래스 병합은 `cn()`(clsx+tailwind-merge).\n3. 반응형 명세 — **모바일 퍼스트**(`min-width` 확장, `max-width` 쿼리 금지).\n4. 다크모드 토큰 + 화면별 레이아웃.\n\n## 작업 원칙\n- **토큰이 SoT** — `!important`·CSS-in-JS·인라인 매직 값 금지. 의미 토큰(primary/surface/foreground 등)으로 표현.\n- **접근성 내장** — 색상 대비 본문 ≥ **4.5:1**, 큰 텍스트 ≥ 3:1(WCAG AA). 포커스 가시 스타일 토큰 정의. 인터랙티브 타깃 ≥ 24×24px.\n- **모바일 퍼스트** — 375px 기준 시작, 브레이크포인트로 확장. 터치/마우스 양쪽 고려, `:active`/`:hover`/`:focus-visible` 상태 정의.\n- **시맨틱 우선** — 디자인을 `<div>` 더미가 아닌 시맨틱 요소(button/nav/header) 전제로 명세.\n- **FSD 미채택** — 컴포넌트를 entities/widgets/shared 레이어로 강제 분해하지 않는다. 범용 UI는 `src/components/`, 유틸은 `src/lib/`.\n\n## 컨텍스트 로드 (있으면 우선)\n- `_workspace/spec.md`의 `project.context`(톤앤매너)·다크모드·언어 설정과 `*_notes`를 Read해 반영. 모순 모호 시 `AskUserQuestion`.\n\n## 출력 — `_workspace/03_design_system.md` (+ `tailwind.config` 또는 `@theme` 토큰 갱신)\n```markdown\n# 디자인 시스템: {프로젝트}\n## 브랜드 톤 — 분위기 / 레퍼런스 2~3개\n## 컬러 팔레트 (대비비 명시)\n| Token | Light | Dark | Usage | 대비(vs bg) |\n| primary | #3E26FD | #8B7BFF | CTA | 4.5:1+ |\n## 타이포 | Style | Size | Weight | Line Height | Usage |\n## 스페이싱 — 4px 기반: xs(4) sm(8) md(16) lg(24) xl(32) 2xl(48)\n## 컴포넌트 명세 (예: Button)\n- Variants: primary/secondary/outline/ghost/danger · Sizes: sm/md/lg · States: default/hover/active/focus-visible/disabled/loading\n- 예: `cn('bg-primary text-primary-foreground rounded-card px-4 h-10 focus-visible:ring-2')`\n## 반응형 — 브레이크포인트별 레이아웃 변화(모바일 퍼스트)\n## 디자인 가드레일 (Do / Don't)\n- Do: \"primary는 CTA에만\", \"대비 4.5:1 미만 텍스트 금지\"\n- Don't: \"focus 링 제거 금지\", \"max-width 미디어쿼리 금지\", \"폰트 웨이트 3개 이상 혼용 금지\"\n```\n\n## Grading Criteria (4축, 미달 시 재작업)\nDesign Quality(30%, ≥7) · Originality(25%, ≥6) · Craft(25%, 대비·스페이싱 정밀 ≥7) · Functionality(20%, 정보위계·a11y ≥8). Originality ≤5면 재작업.\n\n## 핸드오프\n- product-planner로부터: 화면 구조·기능 요구사항.\n- web-inspector에게: 디자인 토큰·대비 기준(디자인 QA 대조 기준).\n- UI 구현 단계에: 컴포넌트 명세·반응형 레이아웃.\n"}},"vitest":{"manifest":{"name":"vitest","deps":["vitest","@vitejs/plugin-react","jsdom","@testing-library/react","@testing-library/jest-dom","@testing-library/user-event"],"husky":false,"questions":[],"targets":[{"src":"vitest.config.ts","dest":"vitest.config.ts","kind":"static","createOnly":true},{"src":"tests/setup.ts","dest":"tests/setup.ts","kind":"static","createOnly":true},{"src":"vitest.md","dest":".claude/rules/vitest.md","kind":"doc"}]},"files":{"tests/setup.ts":"// 스캐폴드(ccx) — 전역 테스트 셋업. jest-dom 매처(toBeInTheDocument 등) 등록.\nimport '@testing-library/jest-dom/vitest';\n","vitest.config.ts":"import react from '@vitejs/plugin-react';\nimport { defineConfig } from 'vitest/config';\n\n// 스캐폴드(ccx) — 프로젝트 소유. React 컴포넌트/유닛 테스트(jsdom).\nexport default defineConfig({\n  plugins: [react()],\n  resolve: {\n    alias: { '@': new URL('./src', import.meta.url).pathname },\n  },\n  test: {\n    environment: 'jsdom',\n    globals: true,\n    setupFiles: ['./tests/setup.ts'],\n    include: ['tests/**/*.test.{ts,tsx}', 'src/**/*.test.{ts,tsx}'],\n    coverage: {\n      reporter: ['text', 'html'],\n      exclude: ['node_modules/', 'tests/', '**/*.config.*', '.next/'],\n    },\n  },\n});\n","vitest.md":"<!-- Managed by ccx -->\n# 테스트 (Vitest + Testing Library)\n\n> 단일 테스트 러너는 Vitest. React 컴포넌트는 Testing Library(RTL)로 **동작**을 검증한다.\n\n## DO\n1. **행동을 테스트** — 구현 디테일(state·내부 함수) 말고 사용자 관점 동작. RTL 쿼리는 **role/label 우선**(`getByRole`/`getByLabelText`), `getByTestId`는 최후.\n2. **도메인 로직은 렌더러 없이** — 순수 함수/서비스(`architecture.md`)는 jsdom 없이 빠른 단위 테스트. 컴포넌트 테스트보다 우선.\n3. 비동기는 `findBy*`/`waitFor`. 사용자 상호작용은 `@testing-library/user-event`.\n4. 네트워크는 모킹(MSW 권장) — 실제 호출 금지.\n\n## DON'T\n1. 스냅샷 남발 금지(깨지기 쉽고 의미 약함) — 핵심 출력만 선별 스냅샷.\n2. `act` 경고 무시 금지. 구현 변경마다 깨지는 과結합 테스트 금지.\n3. 커버리지 숫자를 목표로 무의미 테스트 추가 금지.\n\n## 메모\n- 설정: `vitest.config.ts`(jsdom·`@` alias)·`tests/setup.ts`(jest-dom 매처). 테스트 위치: `tests/**` 또는 `src/**/*.test.tsx`.\n- Biome는 test 파일에서 `noConsole`/`noExplicitAny`를 완화(설정 override).\n"}}}}}; // { pack: { meta:{name,description,requires,concern}, modules:{mod:{manifest,files}} } }
const VERSION = '2.1.2';
const REPO = 'imDangerous/claude-code-extensions';

const MARK_START = '# >>> ccx';
const MARK_END = '# <<< ccx <<<';
const SENTINEL = 'Managed by ccx';
const CONFIG = join('.claude', 'extends', 'config.json');
const CLAUDE = 'CLAUDE.md';
const CM_START = '<!-- ccx:managed:start -->';
const CM_END = '<!-- ccx:managed:end -->';
const MARK_ALWAYS = '<!-- ccx:always -->'; // 이 HTML 주석 마커가 있는 룰만 always-on(@import), 없으면 색인(on-demand). 산문에 'ccx:always' 언급해도 오분류 안 되도록 전체 주석으로 매칭.

const c = {
  ok: (m) => console.log(`\x1b[32m[✓]\x1b[0m ${m}`),
  warn: (m) => console.log(`\x1b[33m[⚠]\x1b[0m ${m}`),
  err: (m) => console.error(`\x1b[31m[✗]\x1b[0m ${m}`),
  info: (m) => console.log(`\x1b[36m[i]\x1b[0m ${m}`),
  plain: (m) => console.log(m),
};
const sha = (s) => createHash('sha256').update(s).digest('hex');
const read = (p) => readFileSync(p, 'utf8');
const write = (p, s) => {
  mkdirSync(dirname(p), { recursive: true });
  writeFileSync(p, s);
};
const eol = (s) => s.replace(/\r\n/g, '\n');

function parseArgs(argv) {
  const out = { _: [], flags: {} };
  for (let i = 0; i < argv.length; i++) {
    const a = argv[i];
    if (a.startsWith('--')) {
      const k = a.slice(2);
      const n = argv[i + 1];
      if (n && !n.startsWith('--')) { out.flags[k] = n; i++; } else out.flags[k] = true;
    } else out._.push(a);
  }
  return out;
}

function detectPM(root) {
  if (existsSync(join(root, 'pnpm-lock.yaml'))) return 'pnpm';
  if (existsSync(join(root, 'yarn.lock'))) return 'yarn';
  if (existsSync(join(root, 'bun.lockb'))) return 'bun';
  if (existsSync(join(root, 'package-lock.json'))) return 'npm';
  return 'npm';
}
const installCmd = (pm, d) => ({ pnpm: `pnpm add -D ${d.join(' ')}`, npm: `npm install -D ${d.join(' ')}`, yarn: `yarn add -D ${d.join(' ')}`, bun: `bun add -d ${d.join(' ')}` })[pm];
const prodInstallCmd = (pm, d) => ({ pnpm: `pnpm add ${d.join(' ')}`, npm: `npm install ${d.join(' ')}`, yarn: `yarn add ${d.join(' ')}`, bun: `bun add ${d.join(' ')}` })[pm];
const ciInstall = (pm) => ({ pnpm: 'pnpm install --frozen-lockfile --ignore-scripts', npm: 'npm ci --ignore-scripts', yarn: 'yarn install --frozen-lockfile --ignore-scripts', bun: 'bun install --frozen-lockfile --ignore-scripts' })[pm];
const execPrefix = (pm) => ({ pnpm: 'pnpm exec', npm: 'npm exec', yarn: 'yarn', bun: 'bunx' })[pm];
// PM별 로컬 bin 실행 — Yarn PnP/pnpm에서 ./node_modules/.bin 경로가 깨지므로 PM 런처 경유.
const binRun = (pm, bin) => ({ pnpm: `pnpm exec ${bin}`, npm: `npx --no-install ${bin}`, yarn: `yarn ${bin}`, bun: `bunx ${bin}` })[pm];

// ── pack/config ───────────────────────────────────────────────────
function resolvePacks(name, seen = new Set(), order = []) {
  if (seen.has(name)) return order;
  if (!CATALOG[name]) throw new Error(`알 수 없는 pack: ${name}`);
  seen.add(name);
  for (const dep of CATALOG[name].meta.requires || []) resolvePacks(dep, seen, order);
  order.push(name);
  return order;
}
const modulesOf = (pack) => Object.entries(CATALOG[pack].modules); // [[name, {manifest,files}]]
function unionQuestions(packs) {
  const seen = new Set(), qs = [];
  for (const pk of packs) for (const [, m] of modulesOf(pk)) for (const q of m.manifest.questions || []) {
    if (seen.has(q.key)) continue;
    seen.add(q.key); qs.push(q);
  }
  return qs;
}
function readConfig(root) {
  const p = join(root, CONFIG);
  if (!existsSync(p)) return null;
  try { return JSON.parse(read(p)); } catch { return null; }
}
const resolvePM = (cfg, root) => (cfg.packageManager && cfg.packageManager !== 'auto' ? cfg.packageManager : detectPM(root));

// ── render / classify / write (id = pack/module) ──────────────────
function render(t, files, cfg, pm, id) {
  id = t.blockId || id; // 훅 블록 마커 id 고정(모듈 이동 후 마이그레이션) — 예: js/git-hooks가 core/git 블록 유지
  let s = files[t.src].replaceAll('__CCX_ID__', id);
  if (t.placeholders) for (const [ph, v] of Object.entries(t.placeholders)) s = s.replaceAll(ph, v === '@ciInstall' ? ciInstall(pm) : typeof v === 'string' && v.startsWith('@bin:') ? binRun(pm, v.slice(5)) : v);
  if (t.blocks) for (const b of t.blocks) {
    if (b.equals !== undefined ? cfg[b.enabledIf] === b.equals : cfg[b.enabledIf]) continue;
    const L = s.split('\n');
    const st = L.findIndex((l) => l.includes(`ccx: ${b.name}`));
    const en = L.findIndex((l, i) => i > st && l.trim() === MARK_END);
    if (st !== -1 && en !== -1) L.splice(st, en - st + 1);
    s = L.join('\n');
  }
  return s;
}
function findBlock(lines, id) {
  const s = lines.findIndex((l) => l.startsWith(MARK_START) && l.includes(`ccx:${id}`));
  if (s === -1) return [-1, -1];
  return [s, lines.findIndex((l, i) => i >= s && l.trim() === MARK_END)];
}
function extractBlock(content, id) {
  const L = eol(content).split('\n');
  const [s, e] = findBlock(L, id);
  return s === -1 || e === -1 ? null : L.slice(s, e + 1).join('\n');
}
// soleOwner 훅: 현재 id 외의 ccx 블록은 stale(예: v1 `rules/git`, 구 `core/git`) — 감지/제거로 마이그레이션 갭 self-heal.
const hasForeignCcxBlock = (lines, keepId) => lines.some((l) => l.startsWith(MARK_START) && !l.includes(`ccx:${keepId}`));
function stripForeignCcxBlocks(text, keepId) {
  const L = eol(text).split('\n');
  for (let i = 0; i < L.length; i++) {
    if (L[i].startsWith(MARK_START) && !L[i].includes(`ccx:${keepId}`)) {
      const e = L.findIndex((l, j) => j >= i && l.trim() === MARK_END);
      if (e !== -1) { L.splice(i, e - i + 1); i--; }
    }
  }
  return L.join('\n');
}
function classify(root, t, rendered, id) {
  id = t.blockId || id;
  const p = join(root, t.dest);
  if (!existsSync(p)) return 'CREATE';
  if (t.createOnly) return 'KEEP';
  const cur = eol(read(p)), ren = eol(rendered);
  if (t.kind === 'hook') {
    const foreign = t.soleOwner && hasForeignCcxBlock(cur.split('\n'), id); // stale 블록(레거시 id) 존재 → 정리 필요
    const mine = extractBlock(cur, id);
    if (mine !== null) return mine === extractBlock(ren, id) && !foreign ? 'IDENTICAL' : 'UPDATE';
    return foreign ? 'UPDATE' : 'MERGE';
  }
  if (sha(cur) === sha(ren)) return 'IDENTICAL';
  if (cur.includes(SENTINEL)) return 'UPDATE';
  return 'FOREIGN';
}
const targetEnabled = (t, cfg) => (!t.enabledIf ? true : t.equals !== undefined ? cfg[t.enabledIf] === t.equals : !!cfg[t.enabledIf]);
const activeTargets = (mf, cfg) => mf.targets.filter((t) => targetEnabled(t, cfg));
function writeTarget(root, t, rendered, status, id) {
  id = t.blockId || id;
  const p = join(root, t.dest);
  if (t.kind === 'hook') {
    if (!existsSync(p)) { write(p, rendered); chmodSync(p, 0o755); return; }
    let body = read(p);
    if (t.soleOwner) body = stripForeignCcxBlocks(body, id); // 레거시/외부 ccx 블록 제거(마이그레이션)
    const cur = body.split('\n');
    const [s, e] = findBlock(cur, id);
    if (s !== -1 && e !== -1) cur.splice(s, e - s + 1, extractBlock(rendered, id)); // 제자리 갱신
    else { cur.push('', extractBlock(rendered, id)); } // 없으면 추가(MERGE)
    write(p, `${cur.join('\n').replace(/\n{3,}/g, '\n\n').replace(/\s*$/, '')}\n`);
    chmodSync(p, 0o755);
    return;
  }
  if (status === 'FOREIGN' && existsSync(p)) { writeFileSync(`${p}.ccx-bak`, read(p)); c.warn(`백업: ${t.dest}.ccx-bak`); }
  write(p, rendered);
  if (t.exec) chmodSync(p, 0o755);
}
function hookConflict(root) {
  let hp = '';
  try { hp = execSync('git config --get core.hooksPath', { cwd: root, encoding: 'utf8', stdio: ['pipe', 'pipe', 'ignore'] }).trim(); } catch {}
  const x = [];
  if (hp && !hp.startsWith('.husky')) x.push(`core.hooksPath=${hp}`);
  if (existsSync(join(root, 'lefthook.yml'))) x.push('lefthook');
  if (existsSync(join(root, '.pre-commit-config.yaml'))) x.push('pre-commit');
  return x;
}
function isIgnored(root, rel) {
  try { execFileSync('git', ['check-ignore', rel], { cwd: root, stdio: ['pipe', 'pipe', 'ignore'] }); return true; } catch { return false; }
}

// ── questions ─────────────────────────────────────────────────────
async function resolveConfig(questions, args, existing) {
  const f = args.flags, yes = !!f.yes, cfg = {};
  let rl;
  if (!yes) rl = createInterface({ input: process.stdin, output: process.stdout });
  for (const q of questions) {
    const cur = existing && q.key in existing ? existing[q.key] : q.default;
    let v = cur;
    if (q.flag && q.flag in f) v = q.type === 'bool' ? !q.flagInverts : String(f[q.flag]);
    else if (!yes) {
      if (q.type === 'bool') { const a = (await rl.question(`? ${q.prompt} ${cur ? 'Y/n' : 'y/N'} `)).trim().toLowerCase(); v = a ? a === 'y' : cur; }
      else { const a = (await rl.question(`? ${q.prompt} (${cur || ''}) `)).trim(); v = a || cur; }
    }
    cfg[q.key] = v;
  }
  if (rl) rl.close();
  return cfg;
}
function ensurePrepare(root) {
  const pp = join(root, 'package.json');
  if (!existsSync(pp)) return;
  const pkg = JSON.parse(read(pp));
  pkg.scripts ||= {};
  if (!(pkg.scripts.prepare || '').includes('husky')) { pkg.scripts.prepare = pkg.scripts.prepare ? `${pkg.scripts.prepare} && husky` : 'husky'; write(pp, `${JSON.stringify(pkg, null, 2)}\n`); }
}

// ── commands ──────────────────────────────────────────────────────
function eachModule(packs, only, targetPack) {
  const out = [];
  for (const pk of packs) for (const [mn, m] of modulesOf(pk)) {
    if (only && pk === targetPack && !only.includes(mn)) continue;
    out.push({ pk, mn, m, id: `${pk}/${mn}` });
  }
  return out;
}

function checkSet(root, list, cfg, pm, quiet) {
  let conflicts = 0, hard = 0;
  if (list.some(({ m }) => m.manifest.husky) && hookConflict(root).length) { c.err(`훅 매니저 충돌: ${hookConflict(root).join(', ')}`); hard++; }
  for (const { m, id } of list) for (const t of activeTargets(m.manifest, cfg)) {
    const st = classify(root, t, render(t, m.files, cfg, pm, id), id);
    const line = `${t.dest.padEnd(46)} ${st}`;
    if (st === 'FOREIGN') { c.err(`${line} — 외부 내용`); conflicts++; }
    else if (st === 'MERGE') c.warn(`${line} — 관리 블록 추가`);
    else if (st === 'UPDATE') c.warn(`${line} — 갱신 예정`);
    else if (!quiet) c.ok(line);
  }
  if (!quiet) c.plain(`요약: 충돌 ${conflicts} · 하드충돌 ${hard}`);
  return hard ? 2 : conflicts ? 1 : 0;
}

async function cmdInit(root, packName, args) {
  const packs = resolvePacks(packName);
  c.info(`의존성 해소: ${packs.join(' → ')}`);
  const only = args.flags.only ? String(args.flags.only).split(',') : null;
  const list = eachModule(packs, only, packName);
  const cfg = await resolveConfig(unionQuestions(packs), args, readConfig(root));
  const pm = resolvePM(cfg, root);

  const code = checkSet(root, list, cfg, pm, false);
  if (code === 2) { c.err('하드 충돌 — 중단.'); return 2; }
  if (code === 1 && args.flags.yes && !args.flags.force) { c.err('충돌 — --force 없이 비대화형 중단.'); return 1; }
  if (args.flags['dry-run']) { c.info('--dry-run: 종료'); return 0; }

  if (isIgnored(root, CONFIG)) c.warn(`.claude/ 가 gitignore — '!.claude/extends/' 권장`);
  write(join(root, CONFIG), `${JSON.stringify(cfg, null, 2)}\n`);
  c.ok(CONFIG);

  ensurePrepare(root);
  const deps = [...new Set(list.flatMap(({ m }) => m.manifest.deps || []))];
  const prodDeps = [...new Set(list.flatMap(({ m }) => m.manifest.prodDeps || []))];
  const needHusky = list.some(({ m }) => m.manifest.husky);
  if (!args.flags['no-install'] && (deps.length || prodDeps.length)) {
    if (prodDeps.length) { c.info(`deps: ${prodInstallCmd(pm, prodDeps)}`); execSync(prodInstallCmd(pm, prodDeps), { cwd: root, stdio: 'inherit' }); }
    if (deps.length) { c.info(`devDeps: ${installCmd(pm, deps)}`); execSync(installCmd(pm, deps), { cwd: root, stdio: 'inherit' }); }
    if (needHusky) { c.info('husky'); execSync(`${execPrefix(pm)} husky`, { cwd: root, stdio: 'inherit' }); }
  } else if (args.flags['no-install']) c.warn('--no-install: deps/husky 생략');

  for (const { m, id, pk } of list) for (const t of activeTargets(m.manifest, cfg)) {
    const r = render(t, m.files, cfg, pm, id);
    const st = classify(root, t, r, id);
    if (st === 'IDENTICAL' || st === 'KEEP') continue;
    if (st === 'FOREIGN' && !args.flags.force) { c.warn(`${t.dest} — 외부 내용 스킵(--force)`); continue; }
    writeTarget(root, t, r, st, id);
    c.ok(`${t.dest}  (${pk})`);
  }
  c.ok(`완료 — ${packs.join('+')} 설치`);
  c.info('룰을 CLAUDE.md에 연결: `ccx apply`');
  return 0;
}

function cmdApply(root) {
  const rd = join(root, '.claude', 'rules');
  // 2계층: ccx:always 마커 룰만 @import(always-on), 나머지는 색인만(필요 시 읽기) — always-on 컨텍스트 최소화.
  const always = [], ondemand = [];
  if (existsSync(rd)) for (const f of readdirSync(rd).sort()) {
    if (!f.endsWith('.md')) continue;
    const body = read(join(rd, f));
    if (!body.includes(SENTINEL)) continue;
    if (body.includes(MARK_ALWAYS)) always.push(`@.claude/rules/${f}`);
    else {
      const title = (body.split('\n').find((l) => l.startsWith('# ')) || `# ${f}`).slice(2).trim();
      ondemand.push(`- \`.claude/rules/${f}\` — ${title}`);
    }
  }
  const lines = [CM_START, '## AI 표준 (ccx 관리 — 직접 수정 금지. `ccx apply`로 갱신)', ...always];
  if (ondemand.length) lines.push('', '### 상세 규약 (해당 영역 작업 시 해당 파일을 읽는다 — always-on 아님)', ...ondemand);
  lines.push(CM_END);
  const block = lines.join('\n');
  const cp = join(root, CLAUDE);
  let content = existsSync(cp) ? read(cp) : `# ${basename(root)}\n`;
  if (content.includes(CM_START)) {
    const L = content.split('\n');
    const s = L.indexOf(CM_START), e = L.indexOf(CM_END);
    L.splice(s, e - s + 1, block);
    content = L.join('\n');
  } else content = `${block}\n\n${content}`;
  write(cp, content);
  c.ok(`CLAUDE.md 갱신 — always-on ${always.length}개 @import + 상세 색인 ${ondemand.length}개`);
  return 0;
}

function cmdGeneric(root, packName, cmd, args) {
  const packs = resolvePacks(packName);
  const only = args.flags.only ? String(args.flags.only).split(',') : null;
  const fullList = eachModule(packs, only, packName); // check/doctor: 의존(core/js) 포함 — 건강 오판 방지
  const targetList = eachModule([packName], only, packName); // update/remove: 대상 pack만
  const cfg = readConfig(root) || Object.fromEntries(unionQuestions(packs).map((q) => [q.key, q.default]));
  const pm = resolvePM(cfg, root);
  if (cmd === 'check') return checkSet(root, fullList, cfg, pm, false);
  if (cmd === 'doctor') {
    let issues = 0;
    for (const { m, id } of fullList) for (const t of activeTargets(m.manifest, cfg)) {
      const st = classify(root, t, render(t, m.files, cfg, pm, id), id);
      if (st === 'IDENTICAL' || st === 'KEEP') c.ok(t.dest);
      else { c.warn(`${t.dest} — ${st === 'CREATE' ? '없음' : st}`); issues++; }
    }
    c.plain(issues ? `이슈 ${issues}건 — 'ccx ${packName} update'` : '모두 정상');
    return issues ? 1 : 0;
  }
  if (cmd === 'update') {
    for (const { m, id, pk } of targetList) for (const t of activeTargets(m.manifest, cfg)) {
      const r = render(t, m.files, cfg, pm, id), st = classify(root, t, r, id);
      if (st === 'IDENTICAL' || st === 'KEEP') continue;
      if (st === 'FOREIGN') { c.warn(`${t.dest} — 외부 내용 스킵`); continue; }
      writeTarget(root, t, r, st, id); c.ok(`${t.dest} (${pk})`);
    }
    c.ok('update 완료'); return 0;
  }
  if (cmd === 'remove') {
    for (const { m, id } of targetList) for (const t of m.manifest.targets) {
      const p = join(root, t.dest);
      if (!existsSync(p)) continue;
      if (t.kind === 'hook') {
        const cur = read(p).split('\n'); const [s, e] = findBlock(cur, t.blockId || id);
        if (s === -1) { c.warn(`skip ${t.dest}`); continue; }
        cur.splice(s, e - s + 1);
        const rest = cur.join('\n').trim();
        const other = cur.some((l) => l.startsWith(MARK_START));
        if (rest && (other || rest !== '#!/usr/bin/env sh')) { write(p, `${rest}\n`); c.ok(`removed block ${t.dest}`); }
        else { rmSync(p); c.ok(`removed ${t.dest}`); }
      } else if (read(p).includes(SENTINEL)) { rmSync(p); c.ok(`removed ${t.dest}`); }
      else c.warn(`skip ${t.dest} (외부)`);
    }
    c.ok('remove 완료'); return 0;
  }
}

const HELP = `ccx v${VERSION}  (claude-code-extensions)

  ccx <pack> init       팩 설치 (requires 자동 동반: ${Object.keys(CATALOG).join('/')})
  ccx <pack> check|doctor|update|remove   (대상 팩 모듈)
  ccx <pack> <module> <cmd>   단일 모듈 주소지정 (예: ccx core git doctor)
  ccx apply             설치된 룰을 CLAUDE.md 관리 블록에 @import
  ccx list              팩/모듈 목록

옵션: --yes --force --dry-run --no-install --dir <path> --only <m1,m2>  (+ 모듈 플래그)`;

function cmdList() {
  c.plain('packs:');
  for (const [pk, p] of Object.entries(CATALOG)) {
    const req = p.meta.requires?.length ? ` (requires: ${p.meta.requires.join(',')})` : '';
    c.plain(`  ${pk.padEnd(8)}${req}  ${p.meta.description || ''}`);
    c.plain(`    modules: ${Object.keys(p.modules).join(', ')}`);
  }
}

async function main() {
  const a = parseArgs(process.argv.slice(2));
  const first = a._[0];
  const root = resolve(a.flags.dir && a.flags.dir !== true ? String(a.flags.dir) : process.cwd());
  if (!first || first === 'help' || a.flags.help) return c.plain(HELP);
  if (first === 'version' || a.flags.version) return c.plain(VERSION);
  if (first === 'list') return cmdList();
  if (first === 'apply') return process.exit(cmdApply(root));
  if (first === 'self-update') return c.info(`curl -fsSL https://github.com/${REPO}/releases/latest/download/install.mjs | node`);
  if (!CATALOG[first]) { c.err(`알 수 없는 pack: ${first}`); cmdList(); process.exit(1); }
  const CMDS = ['init', 'check', 'doctor', 'update', 'remove'];
  // 모듈 주소지정: `ccx <pack> <module> <cmd>` — a._[1]이 명령이 아니라 모듈명이면 --only로 좁힌다.
  let cmd = a._[1] || 'init';
  if (a._[1] && !CMDS.includes(a._[1])) {
    if (!CATALOG[first].modules[a._[1]]) { c.err(`알 수 없는 모듈: ${first} ${a._[1]}  (모듈: ${Object.keys(CATALOG[first].modules).join(', ')})`); process.exit(1); }
    a.flags.only = a._[1];
    cmd = a._[2] || 'init';
    if (!CMDS.includes(cmd)) { c.err(`알 수 없는 명령: ${cmd}`); c.plain(HELP); process.exit(1); }
  }
  let code = 0;
  if (cmd === 'init') code = await cmdInit(root, first, a);
  else if (['check', 'doctor', 'update', 'remove'].includes(cmd)) code = cmdGeneric(root, first, cmd, a);
  else { c.err(`알 수 없는 명령: ${cmd}`); c.plain(HELP); code = 1; }
  process.exit(code);
}
main().catch((e) => { c.err(e.message); process.exit(1); });
